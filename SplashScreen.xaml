<Window x:Class="DPC.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="SplashScreen"
        Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        Loaded="Window_Loaded"
        FontFamily="{StaticResource Lexend}">

    <Grid>
        <!-- Outer Glow Effect -->
        <Border CornerRadius="20" Background="Transparent">
            <Border.Effect>
                <DropShadowEffect x:Name="GlowEffect" Color="Red" BlurRadius="40" ShadowDepth="0"/>
            </Border.Effect>
        </Border>

        <!-- Card Container -->
        <materialDesign:Card Padding="20" Background="#1E1E1E" UniformCornerRadius="30"
                             Width="500" Height="400"
                             VerticalAlignment="Center" HorizontalAlignment="Center">

            <!-- Center Content -->
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Logo Image -->
                <Image x:Name="LogoImage" Source="/Assets/images/logo.png" Width="160" Height="160" Grid.Row="0"
                       HorizontalAlignment="Center"/>

                <!-- Title -->
                <TextBlock Text="DREAM PC BUILD" FontSize="32" FontWeight="Bold" Foreground="White"
                           HorizontalAlignment="Center" Grid.Row="1" Margin="0,10,0,0"/>

                <!-- Subtitle -->
                <TextBlock Text="Management System" FontSize="18" FontWeight="Regular" Foreground="White"
                           HorizontalAlignment="Center" Grid.Row="2"/>

                <!-- Loading Text -->
                <TextBlock x:Name="LoadingText" Text="Loading..." FontSize="14" Foreground="LightGray"
                           HorizontalAlignment="Center" Grid.Row="3" Margin="0,5,0,0"/>

                <!-- Progress Bar -->
                <ProgressBar x:Name="LoadingProgressBar" Grid.Row="4"
                    Width="200" Height="7"
                    Foreground="White" Background="#333"
                    IsIndeterminate="True"
                    HorizontalAlignment="Center"
                    Margin="0,10,0,0"/>

            </Grid>
        </materialDesign:Card>
    </Grid>
</Window>