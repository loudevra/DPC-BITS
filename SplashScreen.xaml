<Window x:Class="DPC.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="SplashScreen"
        Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        Loaded="Window_Loaded"
        FontFamily="{StaticResource LexendRegular}">

	<Grid>
		<!-- Outer Glow Effect -->
		<Border CornerRadius="20" Background="Transparent">
			<Border.Effect>
				<DropShadowEffect x:Name="GlowEffect" Color="Red" BlurRadius="40" ShadowDepth="0"/>
			</Border.Effect>
		</Border>

		<!-- Card Container -->
		<materialDesign:Card Padding="20" Background="#1E1E1E" UniformCornerRadius="16"
                             VerticalAlignment="Center" HorizontalAlignment="Center">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<!-- Logo Image -->
				<Image x:Name="LogoImage" Source="/Assets/images/logo.png" Width="80" Height="80" Grid.Row="0"/>

				<!-- Title -->
				<TextBlock Text="Dream PC" FontSize="24" FontWeight="Bold" Foreground="White"
                           HorizontalAlignment="Center" Grid.Row="1"/>

				<!-- Subtitle -->
				<TextBlock Text="Management System" FontSize="14" FontWeight="Regular" Foreground="White"
                           HorizontalAlignment="Center" Grid.Row="2"/>

				<!-- Loading Text -->
				<TextBlock x:Name="LoadingText" Text="Loading..." FontSize="14" Foreground="LightGray"
                           HorizontalAlignment="Center" Grid.Row="3" Margin="0,5,0,0"/>

				<!-- Progress Bar -->
				<ProgressBar IsIndeterminate="True" Height="4" Grid.Row="4" Margin="0,10,0,0"
                             Foreground="{DynamicResource PrimaryHueMidBrush}" Width="200"
                             HorizontalAlignment="Center"/>
			</Grid>
		</materialDesign:Card>
	</Grid>
</Window>
