<UserControl x:Class="DPC.Components.ConfirmationModal.ConfirmationModalHRM"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Height="Auto"
             FontFamily="{StaticResource Lexend}"
             Background="Transparent">
    <materialDesign:Card Width="450" Padding="24" Background="White"
                         HorizontalAlignment="Center" VerticalAlignment="Center"
                         UniformCornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" 
                       FontSize="24" 
                       Foreground="#2E466B" 
                       FontWeight="Bold" 
                       Text="Confirmation" 
                       Margin="0,0,0,16"/>

            <!-- Content -->
            <TextBlock Grid.Row="1" 
                       Foreground="#AEAEAE" 
                       FontSize="16" 
                       Margin="0,0,0,24"
                       TextWrapping="Wrap">
                You are about to update the salary for employee &lt;Employee Name&gt; from &lt;Prev. Amount&gt; to &lt;Set Amount&gt;.
            </TextBlock>

            <!-- Buttons -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Cancel Button -->
                <Button Grid.Column="0" 
                        Style="{StaticResource RoundedButtonStyle}" 
                        BorderThickness="1"
                        BorderBrush="#AEAEAE"
                        Background="White" 
                        Foreground="#AEAEAE"
                        Margin="0,0,8,0"
                        Height="40"
                        Click="CancelButton_Click">
                    <TextBlock Text="Cancel" 
                               VerticalAlignment="Center" 
                               HorizontalAlignment="Center" 
                               FontSize="16"/>
                </Button>

                <!-- Confirm Button -->
                <Button Grid.Column="1" 
                        Style="{StaticResource RoundedButtonStyle}" 
                        BorderThickness="0" 
                        Background="#2E466B" 
                        Foreground="White"
                        Margin="8,0,0,0"
                        Height="40"
                        Click="ConfirmButton_Click">
                    <TextBlock Text="Confirm" 
                               VerticalAlignment="Center" 
                               HorizontalAlignment="Center" 
                               FontSize="16"/>
                </Button>
            </Grid>
        </Grid>
    </materialDesign:Card>
</UserControl>