<Window x:Class="DPC.Components.ConfirmationModals.NotificationModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Notifications" Height="450" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Border CornerRadius="8" Background="Transparent">
        <materialDesign:Card Width="350" Height="400" UniformCornerRadius="8" Background="White" VerticalAlignment="Center">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Background="#090909" CornerRadius="8,8,0,0" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Your Notifications" Foreground="White" FontSize="16" FontWeight="Bold" FontFamily="Lexend"/>
                        <TextBlock x:Name="txtUserRole" Text="Role: Loading..." Foreground="#AAAAAA" FontSize="12" FontFamily="Lexend" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                    <ItemsControl x:Name="NotificationList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="15,10">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" Foreground="#333333" FontFamily="Lexend"/>
                                        <TextBlock Text="{Binding Message}" TextWrapping="Wrap" FontSize="12" Foreground="#666666" Margin="0,5,0,0" FontFamily="Lexend"/>
                                        <TextBlock Text="{Binding CreatedAt}" FontSize="10" Foreground="#AAAAAA" Margin="0,5,0,0" HorizontalAlignment="Right" FontFamily="Lexend"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Button Grid.Row="2" Content="Close" Margin="15" Height="40" Background="#EEEEEE" Foreground="Black" 
                        BorderThickness="0" FontFamily="Lexend" Click="BtnClose_Click"/>
            </Grid>
        </materialDesign:Card>
    </Border>
</Window>