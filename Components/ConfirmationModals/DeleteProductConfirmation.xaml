<UserControl x:Class="DPC.Components.ConfirmationModals.DeleteProductConfirmation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Height="Auto"
             FontFamily="{StaticResource Lexend}"
             Background="Transparent">

    <materialDesign:Card Width="600" Padding="20" Background="White"
                            BorderThickness="2" BorderBrush="#D23636"
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            UniformCornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>


            <TextBlock Grid.Row="0" FontSize="32" Foreground="#D23636" FontWeight="Bold" Text="Delete Product?" Margin="0,0,0,0" VerticalAlignment="Center"/>


            <StackPanel Grid.Row="1">

                <TextBlock Margin="0,20,0,0" FontWeight="SemiBold"  FontSize="20" FontFamily="{StaticResource Lexend}" Foreground="#AEAEAE" TextWrapping="Wrap">
                    Confirm deletion of 
                    <TextBlock x:Name="lblProductName">
                        
                    </TextBlock>
                    from stocks. Confirming this action will remove all information about this product.
                </TextBlock>
                <DockPanel Margin="0,20,0,0" LastChildFill="False">
                    <Button DockPanel.Dock="Right" Margin="0,0,0,0" Style="{StaticResource RoundedButtonStyle}" BorderThickness="0" Background="#D23636" Click="ConfirmDeletion_Click" Cursor="Hand">
                        <TextBlock Text="Confirm" Margin="35, 10" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource Lexend}" FontWeight="SemiBold" Foreground="White" FontSize="15"/>
                    </Button>
                    <Button DockPanel.Dock="Right" Margin="0, 0,10,0" VerticalAlignment="Center" Style="{StaticResource RoundedButtonStyle}" BorderThickness="2" Background="Transparent"  BorderBrush="#AEAEAE" Click="CloseDeleteDialog" Cursor="Hand">
                        <TextBlock Text="Close" Margin="35, 10" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource Lexend}" FontWeight="SemiBold" Foreground="#AEAEAE" FontSize="15"/>
                    </Button>
                </DockPanel>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</UserControl>
