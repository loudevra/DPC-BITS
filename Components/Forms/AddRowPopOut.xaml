<UserControl x:Class="DPC.Components.Forms.AddRowPopOut"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    mc:Ignorable="d"
    Height="Auto"
    Width="Auto" 
    Background="White"
    Visibility="Visible"
    FontFamily="{StaticResource Lexend}"
    Foreground="#AEAEAE"
    >

    <Grid Width="Auto"> 
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="5,5,5,0">
            <Button Width="Auto" HorizontalContentAlignment="Left" BorderThickness="0" Background="White">
                <StackPanel Width="Auto"  Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="TableRowAddAfter" Width="25" Height="20" Foreground="#AEAEAE" VerticalAlignment="Center"/>
                    <TextBlock Text="Add Rows" FontFamily="{StaticResource Lexend}" Foreground="#AEAEAE" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="5,5,5,0">
            <Button Width="Auto" HorizontalContentAlignment="Left" BorderThickness="0" Background="White">
                <StackPanel Width="Auto"  Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="TableRowRemove" Width="25" Height="20" Foreground="#AEAEAE" VerticalAlignment="Center"/>
                    <TextBlock Text="Remove Rows" FontFamily="{StaticResource Lexend}" Foreground="#AEAEAE" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="5">
            <Button Width="Auto" HorizontalContentAlignment="Left" BorderThickness="0" Background="White">
                <StackPanel Width="Auto"  Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="FormTextbox" Width="25" Height="20" Foreground="#AEAEAE" VerticalAlignment="Center"/>
                    <TextBlock Text="Import Serial Numbers" FontFamily="{StaticResource Lexend}" Foreground="#AEAEAE" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
