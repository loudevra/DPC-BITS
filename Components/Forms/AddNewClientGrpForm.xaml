<UserControl x:Class="DPC.Components.Forms.AddNewClientGrpForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    mc:Ignorable="d"
    d:DesignHeight="400" d:DesignWidth="400" Background="Transparent"
    Visibility="Visible">
    <!-- 🔹 Now Visible by Default -->

    <!-- Full-Screen Overlay -->
    <Grid Background="#99000000" Margin="-17,0,0,0">

        <!-- Modal Box -->
        <Border Background="#FDFDFD" CornerRadius="15" Padding="20" Width="417"
                VerticalAlignment="Center" HorizontalAlignment="Center">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <DockPanel Grid.Row="0">
                    <materialDesign:PackIcon Kind="AccountMultiplePlus" Foreground="#555555" Width="30" Height="30" Margin="0,0,5,0"/>
                    <TextBlock Text="Add New Client Group" FontFamily="https://fonts.googleapis.com/css2?family=Lexend:wght@300;400;700" FontSize="18" FontWeight="SemiBold" Foreground="#555555" Margin="6,5,-1,0" RenderTransformOrigin="0.587,0.571"/>

                    <!-- Close Button -->
                    <materialDesign:PackIcon Kind="Close" Foreground="#555555" Width="24" Height="24" Margin="129,0,0,0"/>
                </DockPanel>

                <!-- Group Name Input -->
                <StackPanel Grid.Row="1" Margin="0,15,0,0">
                    <TextBlock Text="Group Name:" Foreground="#555555" FontFamily="https://fonts.googleapis.com/css2?family=Lexend:wght@300;400;700"  FontWeight="Medium" Margin="0,5,0,6"/>
                    <Border CornerRadius="20" BorderBrush="#555555" BorderThickness="2" Background="Transparent">
                        <TextBox x:Name="txtGroupName" Height="35" Padding="5" BorderThickness="0" Background="Transparent"/>
                    </Border>
                </StackPanel>

                <!-- Description Input -->
                <StackPanel Grid.Row="2" Margin="0,10,0,0">
                    <TextBlock Text="Description:" Foreground="#555555" FontFamily="https://fonts.googleapis.com/css2?family=Lexend:wght@300;400;700" FontWeight="Medium" Margin="0,5,0,5"/>
                    <Border CornerRadius="20" BorderBrush="#555555" BorderThickness="2" Background="Transparent">
                        <TextBox x:Name="txtDescription" Height="35" Padding="5" BorderThickness="0" Background="Transparent"/>
                    </Border>
                </StackPanel>

                <!-- Add Group Button -->
                <Button x:Name="btnAddGroup" Grid.Row="3" Content="Add Group" Width="120" Height="35"
                        Background="#52C69D" Foreground="#FDFDFD" FontWeight="SemiBold" Margin="0,22,0,0"
                        HorizontalAlignment="Right" Click="BtnAddGroup_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="20">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
