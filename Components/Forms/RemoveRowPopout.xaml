<UserControl x:Class="DPC.Components.Forms.RemoveRowPopout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    mc:Ignorable="d"
    Height="Auto"
    Width="Auto" 
    Background="White"
    Visibility="Visible"
    FontFamily="{StaticResource Lexend}"
    Foreground="#AEAEAE"
    >

    <Grid Width="Auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="5,15,5,0">
            <TextBlock Text="Rows" FontWeight="SemiBold"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="5,5,5,0">
            <Border BorderThickness="1.5" BorderBrush="#AEAEAE" CornerRadius="10,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="10,2,0,2" Width="100">
                        <TextBox x:Name="TxtRowNum" Text="1" VerticalAlignment="Center" FontWeight="SemiBold" BorderThickness="0" Foreground="#AEAEAE"
                                PreviewTextInput="IntegerOnlyTextInputHandler"
                                DataObject.Pasting="IntegerOnlyPasteHandler"/>
                    </StackPanel>
                    <Button Grid.Column="1" Width="Auto" HorizontalContentAlignment="Left" BorderThickness="1.5,0,0,0" BorderBrush="#AEAEAE" Background="White" Click="Add_Click">
                        <materialDesign:PackIcon Kind="Plus" Width="25" Height="20" Foreground="#AEAEAE" VerticalAlignment="Center"/>
                    </Button>
                    <Button Grid.Column="2" Width="Auto" HorizontalContentAlignment="Left" BorderThickness="1.5,0,0,0" BorderBrush="#AEAEAE" Background="White" Click="Subtract_Click">
                        <materialDesign:PackIcon Kind="Minus" Width="25" Height="20" Foreground="#AEAEAE" VerticalAlignment="Center"/>
                    </Button>
                </Grid>
            </Border>


        </StackPanel>
        <StackPanel Grid.Row="2" Margin="5" HorizontalAlignment="Right">
            <Border CornerRadius="10" Background="#D23636">
                <Button Width="Auto" HorizontalContentAlignment="Right" BorderThickness="0" Background="Transparent" Click="GenerateRows">
                    <StackPanel Width="Auto"  Orientation="Horizontal">
                        <TextBlock Text="Remove" FontFamily="{StaticResource Lexend}" Foreground="White" VerticalAlignment="Center" Margin="20,1"/>
                    </StackPanel>
                </Button>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
