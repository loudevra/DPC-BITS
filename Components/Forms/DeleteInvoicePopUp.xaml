<UserControl x:Class="DPC.Components.Forms.DeleteInvoicePopUp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Height="Auto"
             FontFamily="{StaticResource Lexend}"
             Background="Transparent">

    <materialDesign:Card Width="450" Padding="20" Background="White"
                            BorderThickness="2" BorderBrush="#D23636"
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            UniformCornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>


            <TextBlock Grid.Row="0" FontSize="32" Foreground="#D23636" FontWeight="SemiBold" Text="Delete Invoice?" Margin="0,0,0,0" VerticalAlignment="Center"/>


            <StackPanel Grid.Row="1">

                <TextBlock Margin="0,20,0,0" FontWeight="SemiBold"  FontSize="20" FontFamily="{StaticResource Lexend}" Foreground="#AEAEAE" TextWrapping="Wrap">
                    You are about to delete
                    <TextBlock x:Name="InvoiceName">
                    </TextBlock>
                    . This action cannot be undone. Please confirm if you’d like to proceed..
                </TextBlock>
                <DockPanel Margin="0,50,0,0" LastChildFill="False">
                    <Button DockPanel.Dock="Right" Margin="0,0,0,0" Style="{StaticResource RoundedButtonStyle}" BorderThickness="0" Background="#D23636">
                        <TextBlock Text="Confirm" Margin="35, 10" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource Lexend}" FontWeight="SemiBold" Foreground="White" FontSize="15"/>
                    </Button>
                    <Button DockPanel.Dock="Right" Margin="0, 0,10,0" VerticalAlignment="Center" Style="{StaticResource RoundedButtonStyle}" BorderThickness="2" Background="Transparent"  BorderBrush="#AEAEAE">
                        <TextBlock Text="Close" Margin="35, 10" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource Lexend}" FontWeight="SemiBold" Foreground="#AEAEAE" FontSize="15"/>
                    </Button>
                </DockPanel>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</UserControl>
