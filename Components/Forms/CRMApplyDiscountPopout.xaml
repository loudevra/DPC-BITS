<UserControl x:Class="DPC.Components.Forms.CRMApplyDiscountPopout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Height="Auto" Width="568"
             FontFamily="{StaticResource Lexend}"
             Foreground="#555555"
             Background="Transparent">

    <materialDesign:Card Width="568" Padding="20" Background="White" BorderBrush="#D9D9D9" BorderThickness="3"
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            UniformCornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>


            <Grid Grid.Row="0">
                <Button Grid.Column="1" Width="24" Height="24" HorizontalAlignment="Right" VerticalAlignment="Top" Background="Transparent" BorderBrush="Transparent" x:Name="BtnClose">
                    <materialDesign:PackIcon Kind="Close" Foreground="#555555" Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Button>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,20,0,10" Grid.ColumnSpan="2">
                    <materialDesign:PackIcon Kind="Sale" Width="132.52471923828125" Height="132.52471923828125" Margin="0,0,15,5" HorizontalAlignment="Center"/>
                    <TextBlock FontSize="32px" FontWeight="Bold" Text="Apply Discount" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                    <TextBlock FontSize="20px" FontWeight="DemiBold" Text="Please note that this discount rate will apply to everyone in this group." Margin="0,0,0,0" HorizontalAlignment="Center" TextWrapping="Wrap" Width="400" TextAlignment="Center"/>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="2" Margin="0,40,0,40">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="300">
                    <StackPanel x:Name="MainContent">
                        <StackPanel Orientation="Vertical" Width="512">
                            <TextBlock Text="Discount Rate:" FontSize="20px" FontWeight="SemiBold" Foreground="#555555" VerticalAlignment="Center" Margin="0,0,15,0"/>
                            <Border Style="{StaticResource RoundedBorderStyle}" Margin="0,5,0,20" Width="Auto" BorderBrush="#555555" BorderThickness="3" Height="56" CornerRadius="30">
                                <TextBox x:Name="TxtApplyDiscount" Style="{StaticResource RoundedTextboxStyle}" Text="%" FontSize="20px" Foreground="#AEAEAE" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </StackPanel>

            <StackPanel Grid.Row="3" Width="512">
                <Button Style="{StaticResource RoundedButtonStyle}" BorderThickness="0" Background="#52C69D" Margin="0,10,0,0" Width="Auto" Height="56">
                    <TextBlock Text="Apply Discount" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#FDFDFD" FontSize="20px"/>
                </Button>
                <Button Style="{StaticResource RoundedButtonStyle}" BorderThickness="0" Background="#555555" Margin="0,10,0,10"  Width="Auto" Height="56">
                    <TextBlock Text="Cancel" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#FDFDFD" FontSize="20px"/>
                </Button>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</UserControl>



