<UserControl x:Class="DPC.Components.Forms.DocumentItemsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DPC.DPC.Components.Forms">

    <Grid>
        <DataGrid x:Name="ItemsGrid"
                  IsReadOnly="True"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  Background="Transparent"
                  RowBackground="Transparent"
                  BorderThickness="0, 1, 0, 0"
                  BorderBrush="Black">

            <DataGrid.Columns>
                <DataGridTemplateColumn x:Name="colImage" Header="Image" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Padding="2">
                                <Image Source="{Binding ProductImage}" MaxHeight="50" MaxWidth="50" Stretch="Uniform"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="70">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontFamily" Value="Lexend"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="Description" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Margin="8" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Description}" FontSize="12" FontFamily="Lexend" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding ProductDescription}" FontSize="11" FontFamily="Lexend" Foreground="#555555" 
                                           Margin="0,3,0,0" Visibility="{Binding ProductDescriptionVisibility}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn x:Name="colUnitPrice" Header="Unit Price" Binding="{Binding UnitPrice}" Width="90"/>
                <DataGridTextColumn x:Name="colLinePrice" Header="Line Price" Binding="{Binding LinePrice}" Width="90"/>
            </DataGrid.Columns>

            <DataGrid.CellStyle>
                <Style TargetType="{x:Type DataGridCell}">
                    <Setter Property="BorderBrush" Value="Black"/>
                    <Setter Property="BorderThickness" Value="0, 0, 1, 1"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type DataGridCell}">
                                <Border Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter VerticalAlignment="Center" Margin="3,0"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsHeaderRow}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="MinHeight" Value="55"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsHeaderRow}" Value="True">
                            <Setter Property="Background" Value="#F2F2F2"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridRow">
                                        <Border BorderBrush="Black" BorderThickness="0,0,0,1" Background="{TemplateBinding Background}" Padding="10,8">
                                            <TextBlock Text="{Binding Description, Converter={StaticResource UpperCaseConverter}}" 
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       FontSize="13" FontWeight="Bold" Foreground="#1D3242"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#090909"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontFamily" Value="Lexend"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="5,3"/>
                    <Setter Property="Height" Value="30"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>
    </Grid>
</UserControl>