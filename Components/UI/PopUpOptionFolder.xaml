<UserControl x:Class="DPC.Components.UI.PopUpOptionFolder"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Height="Auto" Width="Auto"
    Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="MainMenuItemStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <materialDesign:Card Grid.Column="1" Background="#0F0F0F" UniformCornerRadius="0" Margin="0" Width="200">
            <StackPanel Orientation="Vertical" Margin="10,5,10,5">

                <Grid Margin="0,8,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <materialDesign:PackIcon Kind="DeleteSweep" Foreground="#FF5252" Width="20" Height="20" Margin="10,0,0,0"/>

                    <TextBlock Grid.Column="1" VerticalAlignment="center">
                        <Hyperlink Click="DeleteFolder_Click" TextDecorations="None">
                            <TextBlock Text="Delete Folder" Style="{StaticResource MainMenuItemStyle}" Foreground="#FF5252"/>
                        </Hyperlink>
                        <TextBlock.ToolTip>
                            <ToolTip Content="Permanently remove this folder and its contents." 
                                     Style="{StaticResource SpeechBubbleToolTip}"
                                     Placement="Top"/>
                        </TextBlock.ToolTip>
                    </TextBlock>
                </Grid>

            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>