<UserControl x:Class="DPC.Components.Dynamic.DynamicDialogs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Button Style - Enhanced with MaterialDesign -->
        <Style x:Key="DialogButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modal Overlay Style - Improved transition -->
        <Style x:Key="ModalOverlay" TargetType="Grid">
            <Setter Property="Background" Value="#80000000"/>
            <Setter Property="Opacity" Value="0"/>
            <Style.Triggers>
                <Trigger Property="Visibility" Value="Visible">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <PowerEase EasingMode="EaseOut" Power="2"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Dialog Card Animation -->
        <Style x:Key="DialogCard" TargetType="materialDesign:Card">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="0.9" ScaleY="0.9"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Visibility, RelativeSource={RelativeSource AncestorType=Grid}}" Value="Visible">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                               From="0.9" To="1" Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <BackEase EasingMode="EaseOut" Amplitude="0.5"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                               From="0.9" To="1" Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <BackEase EasingMode="EaseOut" Amplitude="0.5"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Dynamic Dialog Modal -->
        <Grid x:Name="DialogModalOverlay" Style="{StaticResource ModalOverlay}" Visibility="Collapsed">
            <materialDesign:Card x:Name="DialogCard" Style="{StaticResource DialogCard}"
                               VerticalAlignment="Center" HorizontalAlignment="Center"
                               Width="320" UniformCornerRadius="8"
                               materialDesign:ElevationAssist.Elevation="Dp8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header with dynamic color and content -->
                    <materialDesign:ColorZone x:Name="DialogHeader" Grid.Row="0" Mode="Custom" Background="#6DC067" 
                                            Height="120" CornerRadius="8,8,0,0">
                        <Grid>
                            <Button x:Name="BtnCloseDialog" HorizontalAlignment="Right" VerticalAlignment="Top"
                                    Style="{StaticResource MaterialDesignIconButton}" Margin="0,8,8,0" Click="BtnCloseDialog_Click">
                                <materialDesign:PackIcon Kind="Close" Foreground="White" Width="20" Height="20"/>
                            </Button>
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock x:Name="DialogTitle" Text="Success!" FontSize="24" FontWeight="SemiBold" 
                                          Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                                <materialDesign:Card Background="White" Width="48" Height="48" UniformCornerRadius="24">
                                    <materialDesign:PackIcon x:Name="DialogIcon" Kind="Check" Width="30" Height="30" 
                                                           Foreground="#6DC067" HorizontalAlignment="Center" 
                                                           VerticalAlignment="Center"/>
                                </materialDesign:Card>
                            </StackPanel>
                        </Grid>
                    </materialDesign:ColorZone>

                    <!-- Content -->
                    <TextBlock x:Name="DialogMessage" Grid.Row="1" Text="Your operation was successful."
                              Margin="24,24,24,24" TextAlignment="Center" FontSize="16" TextWrapping="Wrap"/>

                    <!-- Footer with dynamic buttons -->
                    <Grid Grid.Row="2" Margin="24,0,24,24">
                        <StackPanel x:Name="DialogButtonsPanel" Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button x:Name="PrimaryButton" Content="Got It!" Style="{StaticResource DialogButton}"
                                   Click="PrimaryButton_Click" Margin="5,0"/>
                            <Button x:Name="SecondaryButton" Content="Cancel" Style="{StaticResource DialogButton}"
                                   Click="SecondaryButton_Click" Margin="5,0" Visibility="Collapsed"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>