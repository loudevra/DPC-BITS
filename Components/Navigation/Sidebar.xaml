<UserControl x:Class="DPC.Components.Navigation.Sidebar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="namespace:DPC.Components.Navigation"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Height="Auto" Width="250" Background="#121212">

    <UserControl.Resources>
        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" Padding="10" CornerRadius="5">
                            <StackPanel Orientation="Horizontal">
                                <ContentPresenter />
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border x:Name="SidebarContainer" Background="#121212" Width="250" CornerRadius="20" Padding="15">
        <StackPanel Orientation="Vertical" VerticalAlignment="Stretch">

            <!-- Logo and Branding -->
            <StackPanel HorizontalAlignment="Center" Margin="0,10,0,20">
                <Button x:Name="SidebarLogoButton" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <Image Source="/Assets/images/logo.png" Height="60" Width="60"/>
                </Button>
                <TextBlock x:Name="SidebarTitle" Text="DREAM PC BUILD" Foreground="White" FontWeight="Bold" FontSize="14" TextAlignment="Center"/>
                <TextBlock x:Name="SidebarSubtitle" Text="AND IT SOLUTIONS INC." Foreground="White" FontSize="12" TextAlignment="Center"/>
            </StackPanel>

            <!-- Navigation Items -->
            <StackPanel x:Name="SidebarMenu">
                <Button Style="{StaticResource SidebarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Speedometer" Width="24" Height="24"/>
                        <TextBlock Text="Dashboard" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SidebarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Cart" Width="24" Height="24"/>
                        <TextBlock Text="Sales" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SidebarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Layers" Width="24" Height="24"/>
                        <TextBlock Text="Stock" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SidebarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountMultiple" Width="24" Height="24"/>
                        <TextBlock Text="CRM" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SidebarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Briefcase" Width="24" Height="24"/>
                        <TextBlock Text="Project" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SidebarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Account" Width="24" Height="24"/>
                        <TextBlock Text="Accounts" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SidebarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="TicketPercent" Width="24" Height="24"/>
                        <TextBlock Text="Promo Codes" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SidebarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ChartBar" Width="24" Height="24"/>
                        <TextBlock Text="Data &amp; Reports" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SidebarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="DotsGrid" Width="24" Height="24"/>
                        <TextBlock Text="Miscellaneous" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SidebarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountTie" Width="24" Height="24"/>
                        <TextBlock Text="HRM" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <Grid Height="Auto"/>

            <!-- User Profile -->
            <StackPanel x:Name="UserProfile" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,20,0,10">
                <Ellipse Width="40" Height="40" Fill="White"/>
                <StackPanel Margin="10,0,0,0">
                    <TextBlock x:Name="UserName" Text="Username" Foreground="White" FontWeight="Bold"/>
                    <TextBlock x:Name="UserEmail" Text="name@mail.com" Foreground="Gray" FontSize="12"/>
                </StackPanel>
                <materialDesign:PackIcon Kind="Cog" Width="24" Height="24" Foreground="White" Margin="20,5,0,0"/>
            </StackPanel>

            <!-- Logout Button -->
            <Button Style="{StaticResource SidebarButton}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Logout" Width="24" Height="24" Foreground="Red"/>
                    <TextBlock Text="Logout" Foreground="Red" Margin="10,0,0,0"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Border>
</UserControl>
