<UserControl x:Class="DPC.Components.Navigation.TopNavBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="namespace:DPC.Data.Converters.ValueConverter"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             Height="60" Background="Transparent">

    <!-- Top Navigation Grid -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <!-- Left Icons -->
            <ColumnDefinition Width="*"/>
            <!-- Search Bar (Flexible) -->
            <ColumnDefinition Width="Auto"/>
            <!-- Right Icons -->
        </Grid.ColumnDefinitions>

        <!-- Left Icons (POS, Location) -->
        <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,10,0">
            <Button Style="{StaticResource IconButton}" Click="OpenPOS">
                <materialDesign:PackIcon Kind="Cart" Width="24" Height="24"/>
            </Button>
            <Button Style="{StaticResource IconButton}" Click="ChangeLocation">
                <materialDesign:PackIcon Kind="MapMarker" Width="24" Height="24" Margin="10,0,0,0"/>
            </Button>
        </StackPanel>

        <!-- Search Bar (Expands Dynamically) -->
        <!-- Search Bar (Limited to 50% of Navbar Width) -->
        <!-- Search Bar (Limited to 50% of Navbar Width) -->
        <Grid Grid.Column="1" HorizontalAlignment="Left">
            <Border CornerRadius="18" Background="#FDFDFD" Height="35" x:Name="SearchBorder">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SearchBar"
                materialDesign:HintAssist.Hint="Search customer..."
                FontSize="14"
                Padding="15,0,40,0"
                Background="Transparent"
                BorderThickness="0"
                VerticalContentAlignment="Center"
                materialDesign:HintAssist.IsFloating="True"
                Grid.Column="0"
                Width="250"
                Margin="5,0,0,0"/>

                    <Button x:Name="SearchButton" 
                Background="Transparent" 
                BorderThickness="0"
                Width="40" 
                Height="35"
                Click="SearchCustomer"
                Grid.Column="1"
                Margin="0,0,5,0">
                        <materialDesign:PackIcon Kind="Magnify" Width="20" Height="20" Foreground="Gray"/>
                    </Button>
                </Grid>
            </Border>
        </Grid>

        <!-- Right Icons (Notifications, Messages, Clock In/Out) -->
        <StackPanel Orientation="Horizontal" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0">
            <Button Style="{StaticResource IconButton}" Click="ShowNotifications">
                <materialDesign:PackIcon Kind="Bell" Width="24" Height="24"/>
            </Button>
            <Button Style="{StaticResource IconButton}" Click="ShowMessages">
                <materialDesign:PackIcon Kind="Email" Width="24" Height="24" Margin="10,0,0,0"/>
            </Button>
            <Button Style="{StaticResource IconButton}" Click="ToggleClockInOut">
                <materialDesign:PackIcon x:Name="ClockIcon" Kind="ClockOutline" Width="24" Height="24" Margin="10,0,0,0"/>
            </Button>
            <Button Style="{StaticResource IconButton}" Click="MinimizeWindow">
                <materialDesign:PackIcon x:Name="Minimizebtn" Kind="WindowMinimize" Width="24" Height="24" Margin="10,0,0,0"/>
            </Button>
            <Button Style="{StaticResource IconButton}" Click="MaximizeRestoreWindow">
                <materialDesign:PackIcon x:Name="Maximizebtn" Kind="WindowMaximize" Width="24" Height="24" Margin="10,0,0,0"/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
