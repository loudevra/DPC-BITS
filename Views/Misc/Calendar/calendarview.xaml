<UserControl x:Class="calendarview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200">

    <Grid Background="#0A0A0A">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <materialDesign:Card Grid.Row="0" Background="White" UniformCornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Schedule Overview" FontWeight="Bold" FontSize="16" Foreground="Black"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Row="1" Background="White" UniformCornerRadius="8" Padding="15">
                <StackPanel Margin="0,10,0,0">
                    <StackPanel Orientation="Horizontal" Margin="10,0,0,20">
                        <Ellipse Width="14" Height="14" Fill="#DDD6FE" Margin="0,0,12,0"/>
                        <TextBlock Text="Meeting" Foreground="#333" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="10,0,0,20">
                        <Ellipse Width="14" Height="14" Fill="#FECACA" Margin="0,0,12,0"/>
                        <TextBlock Text="Deadline" Foreground="#333" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="10,0,0,20">
                        <Ellipse Width="14" Height="14" Fill="#BBF7D0" Margin="0,0,12,0"/>
                        <TextBlock Text="Salary Payout" Foreground="#333" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="10,0,0,20">
                        <Ellipse Width="14" Height="14" Fill="#BFDBFE" Margin="0,0,12,0"/>
                        <TextBlock Text="Holiday" Foreground="#333" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <Grid Grid.Column="1" Margin="20,20,40,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BtnPrevMonth" Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Background="#333333" BorderBrush="{x:Null}" Margin="0,0,15,0" Width="35" Height="35" Click="BtnPrevMonth_Click">
                        <materialDesign:PackIcon Kind="ChevronLeft" Foreground="White" Width="24" Height="24"/>
                    </Button>
                    <TextBlock x:Name="TxtMonth" Text="MONTH" Foreground="White" FontSize="45" FontWeight="Bold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtYear" Text="YYYY" Foreground="White" FontSize="45" FontWeight="Light" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button x:Name="BtnNextMonth" Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Background="#333333" BorderBrush="{x:Null}" Width="35" Height="35" Click="BtnNextMonth_Click">
                        <materialDesign:PackIcon Kind="ChevronRight" Foreground="White" Width="24" Height="24"/>
                    </Button>
                </StackPanel>
            </Grid>

            <materialDesign:Card Grid.Row="1" Background="#D3D3D3" UniformCornerRadius="10" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <UniformGrid Grid.Row="0" Columns="7" Height="40">
                        <Border Background="#4A4A4A" Margin="2">
                            <TextBlock Text="SUN" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <Border Background="#4A4A4A" Margin="2">
                            <TextBlock Text="MON" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <Border Background="#4A4A4A" Margin="2">
                            <TextBlock Text="TUE" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <Border Background="#4A4A4A" Margin="2">
                            <TextBlock Text="WED" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <Border Background="#4A4A4A" Margin="2">
                            <TextBlock Text="THU" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <Border Background="#4A4A4A" Margin="2">
                            <TextBlock Text="FRI" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <Border Background="#4A4A4A" Margin="2">
                            <TextBlock Text="SAT" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </UniformGrid>

                    <ItemsControl Grid.Row="1" x:Name="CalendarGrid" Margin="0,5,0,0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="7"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{Binding CellBackground}" Margin="2" CornerRadius="4" MinHeight="80">
                                    <Grid Margin="4">
                                        <TextBlock Text="{Binding DayNumber}" FontSize="12" Foreground="#666" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="2,0,0,0" FontWeight="Medium"/>

                                        <ScrollViewer VerticalScrollBarVisibility="Hidden" Margin="0,18,0,0">
                                            <ItemsControl ItemsSource="{Binding DayEvents}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>

                                                        <Button Click="EventBlock_Click" 
                                                                Background="{Binding EventColor}" 
                                                                BorderThickness="0" 
                                                                Margin="0,0,0,4" 
                                                                Cursor="Hand"
                                                                HorizontalContentAlignment="Stretch">

                                                            <Button.Template>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="4,4,8,4">
                                                                        <ContentPresenter />
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Button.Template>

                                                            <StackPanel Orientation="Horizontal">
                                                                <Border Width="3" Height="10" CornerRadius="1.5" Background="#40000000" Margin="2,0,6,0" VerticalAlignment="Center" />
                                                                <TextBlock Text="{Binding Title}" Foreground="#1A1A1A" FontSize="11" FontWeight="Medium" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                                            </StackPanel>

                                                            <Button.ToolTip>
                                                                <ToolTip Background="White" Foreground="Black">
                                                                    <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                                                                </ToolTip>
                                                            </Button.ToolTip>
                                                        </Button>

                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>

                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                    </ItemsControl>
                </Grid>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>