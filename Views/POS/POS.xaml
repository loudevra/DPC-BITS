<UserControl x:Class="DPC.Views.POS.POS"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="820">
    <Grid Background="#D3D3D3" Height="700" VerticalAlignment="Bottom">

        <!-- Product Cards -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,5,0,0" MaxHeight="600">
            <WrapPanel Margin="8" Height="745">
                <!-- 20 Product Cards -->
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
                <Button Width="140" Height="140" Margin="8"
                        Background="White" BorderBrush="#ECECEC" BorderThickness="2"
                        Padding="0" Cursor="Hand"
                        Command="{Binding DataContext.ProductCardClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel>
                        <Border Height="70" Width="124" Margin="8,8,8,0"
                                Background="#F5F5F5" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="{Binding ImageSource}" 
                                   Stretch="UniformToFill"
                                   Width="124" Height="70"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="8,16,8,8">
                            <TextBlock Text="Product"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="₱ 00.00"
                                       FontSize="10"
                                       FontFamily="Lexend"
                                       Foreground="#555555"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</UserControl>