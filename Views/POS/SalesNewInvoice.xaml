<UserControl x:Class="DPC.Views.POS.SalesNewInvoice"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             Background="#D9D9D9"
             FontFamily="Lexend"
             Foreground="#474747">

    <UserControl.Resources>
        <!-- Style for TextBox -->
        <Style x:Key="StyledSearchTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="15,0"/>
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#474747"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Style for Button -->
        <Style x:Key="StyledEnterButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Width" Value="100"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Foreground" Value="#474747"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="BorderBrush" Value="#474747"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border 
                                x:Name="ComboBoxBorder"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="20"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                                <ContentPresenter
                                    Name="ContentSite"
                                    IsHitTestVisible="False"
                                    Content="{TemplateBinding SelectionBoxItem}"
                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                    VerticalAlignment="Center"
                                    Margin="0"/>
                            </Border>
                            <ToggleButton 
                                Name="ToggleButton"
                                IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                Focusable="False"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Width="30"
                                Margin="0,0,5,0">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="OverridesDefaultStyle" Value="True"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border Background="Transparent" BorderThickness="0">
                                                        <materialDesign:PackIcon Kind="ChevronDown" Width="20" Height="20" Foreground="Black"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#D9D9D9" Padding="25,10,25,0">
            <TextBlock Text="New Sales Invoice"
                       FontSize="45"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="25,10,25,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Search Bar -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- TextBox -->
                    <Border Grid.Column="0" Background="White" CornerRadius="20,0,0,20" Height="40" Margin="0,5,0,5" BorderBrush="#474747" BorderThickness="1">
                        <TextBox
                            Style="{StaticResource StyledSearchTextBox}"
                            materialDesign:HintAssist.Hint="Enter product name, code, or scan barcode"
                            MinWidth="400"/>
                    </Border>

                    <!-- ENTER Button -->
                    <Border Grid.Column="1" Background="#555555" CornerRadius="0,20,20,0" Height="40" Margin="0,5,10,5" BorderBrush="#474747" BorderThickness="1">
                        <Button Style="{StaticResource StyledEnterButton}" Content="Enter"/>
                    </Border>

                    <!-- ComboBox -->
                    <Border Grid.Column="2" Background="Transparent" CornerRadius="20" Height="40" Margin="0,5,0,5" ClipToBounds="True">
                        <ComboBox
                            Width="120"
                            FontFamily="Lexend"
                            FontSize="13"
                            Foreground="#474747"
                            Background="White"
                            BorderThickness="1"
                            BorderBrush="#474747"
                            Padding="10,0"
                            VerticalContentAlignment="Center">
                            <ComboBoxItem Content="All" IsSelected="True"/>
                            <ComboBoxItem Content="10"/>
                            <ComboBoxItem Content="25"/>
                            <ComboBoxItem Content="50"/>
                            <ComboBoxItem Content="100"/>
                        </ComboBox>
                    </Border>
                </Grid>

                <!-- ContentControl Placeholder -->
                <ContentControl Grid.Row="1"
                                x:Name="MainContentArea"
                                Margin="0,0,0,0"
                                Height="300"
                                Content="{Binding SomeContent}"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"/>
            </Grid>
        </ScrollViewer>

        <!-- Bottom Buttons -->
        <Border Grid.Row="2" Background="Transparent" Padding="0,15" BorderThickness="0,1,0,0" BorderBrush="#D0D0D0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Center Buttons -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Padding="20,5" Height="40" Background="#ffa87d" Foreground="White" FontWeight="SemiBold" FontFamily="Lexend" Cursor="Hand"
                            x:Name="BtnDraft" Click="DraftControl"
                            BorderBrush="#ffa87d" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ContentSave" Margin="0,0,5,0"/>
                            <TextBlock Text="Draft"/>
                        </StackPanel>
                    </Button>

                    <Button Padding="20,5" Height="40" Background="#16d399" Foreground="White" FontWeight="SemiBold" FontFamily="Lexend" Cursor="Hand"
                            BorderBrush="#16d399" BorderThickness="1" x:Name="BtnPayment" Click="PaymentControl">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Cash" Margin="0,0,5,0"/>
                            <TextBlock Text="Payment"/>
                        </StackPanel>
                    </Button>

                    <Button Padding="20,5" Height="40" Background="#00b5b8" Foreground="White" FontWeight="SemiBold" FontFamily="Lexend" Cursor="Hand"
                            BorderBrush="#00b5b8" BorderThickness="1" x:Name="BtnCard" Click="CardControl">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CreditCard" Margin="0,0,5,0"/>
                            <TextBlock Text="Card"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Farthest Right Button -->
                <Button Grid.Column="1" Background="Transparent" BorderBrush="Transparent" Margin="0,0,10,0" Cursor="Hand" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <materialDesign:PackIcon Kind="ReplyAll" Foreground="Red" Width="15" Height="15"/>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
