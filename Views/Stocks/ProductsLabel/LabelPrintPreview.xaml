<Window x:Class="DPC.Views.Stocks.ProductsLabel.LabelPrintPreview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:DPC"
        mc:Ignorable="d"
        Width="970" Height="600"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="PrinterButton" TargetType="Button">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="#244270"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20" Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="PrinterPosPlus" Width="20" Height="20" Foreground="White" Margin="0,0,5,0"/>
                                <TextBlock Text="{TemplateBinding Content}" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#D9D9D9" BorderBrush="#244270" BorderThickness="4" CornerRadius="15" 
            VerticalAlignment="Stretch" HorizontalAlignment="Stretch">

        <Grid Width="970">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <DockPanel Grid.Row="0" LastChildFill="False" HorizontalAlignment="Stretch">
                <TextBlock Text="Print Preview" Foreground="#555555" VerticalAlignment="Center" FontSize="24" Margin="20" FontFamily="Segoe UI" FontWeight="Bold" HorizontalAlignment="Left"/>
                <Border DockPanel.Dock="Right" CornerRadius="15">
                    <Button BorderThickness="0" IsCancel="True" Background="Transparent">
                        <materialDesign:PackIcon HorizontalAlignment="Right" Kind="Close" Foreground="#555555" Width="35" Height="35" Margin="20" />
                    </Button>
                </Border>
            </DockPanel>

            <!-- Scrollable Content -->
            <ScrollViewer Grid.Row="1" Margin="0,0,0,20" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Hidden">
                <DockPanel LastChildFill="True">
                    <StackPanel DockPanel.Dock="Left" Margin="20">
                        <Border Height="1122.5" Width="793.7" Background="White" Padding="40" x:Name="preview">
                            <!-- dynamically added labels go here -->
                        </Border>
                    </StackPanel>

                    <!-- Sidebar -->
                    <StackPanel DockPanel.Dock="Right" Margin="0,20,20,0" VerticalAlignment="Top">
                        <Button Click="SavePrint" Content="Print" Style="{StaticResource PrinterButton}"/>
                    </StackPanel>
                </DockPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
