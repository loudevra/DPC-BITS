<Window x:Class="DPC.Views.Stocks.ProdductsLabel.CustomLabel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="namespace:DPC.Views.Stocks.ProdductsLabel"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Custom Label"
        Width="1190"
        Height="782"
        Background="#D9D9D9"
        WindowStartupLocation="CenterScreen">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid>
            <Grid.RowDefinitions>
                <!--Product Label-->
                <RowDefinition Height="Auto"/>
                <!--Print Settings-->
                <RowDefinition Height="Auto"/>
                <!--Print Details-->
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel x:Name="ProductLabelRow" Grid.Row="0" Margin="0,0,0,30">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height=".2*"/>
                        <RowDefinition Height=".4*"/>
                        <RowDefinition Height=".4*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="25,25,0,30">
                        <TextBlock Text="Product Label" FontWeight="SemiBold" FontFamily="Lexend" FontSize="25" Foreground="#474747" VerticalAlignment="Top"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Margin="0,0,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width=".5*"/>
                                <ColumnDefinition Width=".5*"/>
                            </Grid.ColumnDefinitions>
                            <!-- Yes Button with Border -->
                            <Border Grid.Column="0" Background="#AEAEAE" CornerRadius="20" Padding="10" Margin="25,0,25,25">
                                <RadioButton Content="Custom" IsChecked="True" GroupName="Custom_Standard" FontFamily="Lexend" FontSize="14" Foreground="White">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderBrush="White" BorderThickness="2" Margin="5">
                                                                <Ellipse Width="8" Height="8" Fill="White" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <ContentPresenter VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>
                            </Border>

                            <!-- No Button with Border -->
                            <Border Grid.Column="1" Background="#AEAEAE" CornerRadius="20" Padding="10" Margin="25,0,25,25">
                                <RadioButton Content="Standard" GroupName="Custom_Standard" FontFamily="Lexend" FontSize="14" Foreground="White">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderBrush="White" BorderThickness="2" Margin="5">
                                                                <Ellipse Width="8" Height="8" Fill="White" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <ContentPresenter VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>
                            </Border>
                        </Grid>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Margin="25,0,25,0">
                        <TextBlock Text="Warehouse:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top" Margin="0,0,0,5"/>
                        <StackPanel Width="Auto" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                            <ComboBox SelectedIndex="0" FontFamily="Lexend" FontSize="14" Foreground="#474747"
    Padding="10" Width="Auto" Height="40" Background="White" BorderBrush="black"
    BorderThickness="1" HorizontalContentAlignment="Left" Margin="0,0,0,10">
                                <ComboBox.Resources>
                                    <!-- Custom ComboBox Toggle Button -->
                                    <Style x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
                                        <Setter Property="Focusable" Value="False"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border Background="Transparent" Padding="5">
                                                        <Grid>
                                                            <Path x:Name="Arrow" Data="M 0 0 L 4 4 L 8 0 Z"
              HorizontalAlignment="Right" VerticalAlignment="Center"
              Fill="Black" Width="8" Height="8" 
              Margin="0,5,10,0"/>
                                                        </Grid>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>

                                    <!-- Custom ComboBox Style -->
                                    <Style TargetType="ComboBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ComboBox">
                                                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="20" Padding="5">
                                                        <Grid>
                                                            <!-- Toggle Button for Dropdown -->
                                                            <ToggleButton Name="ToggleButton" 
                                      Style="{StaticResource ComboBoxToggleButton}"
                                      Grid.Column="2" Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" 
                                      ClickMode="Press"/>

                                                            <!-- Selected Content -->
                                                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" 
                                      Content="{TemplateBinding SelectionBoxItem}" 
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      Margin="10,0,40,0" VerticalAlignment="Center"/>

                                                            <!-- Dropdown List -->
                                                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" 
                                       AllowsTransparency="True" Focusable="False"
                                       PopupAnimation="Slide"
                                       MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                                                <Border Background="White" BorderBrush="Gray" BorderThickness="1" 
                                        CornerRadius="10"
                                        Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                                                    <ScrollViewer>
                                                                        <StackPanel>
                                                                            <ItemsPresenter/>
                                                                        </StackPanel>
                                                                    </ScrollViewer>
                                                                </Border>
                                                            </Popup>
                                                        </Grid>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ComboBox.Resources>

                                <!-- ComboBox Items -->
                                <ComboBoxItem Content="Warehouse 1"/>
                                <ComboBoxItem Content="Warehouse 2"/>
                            </ComboBox>
                        </StackPanel>
                        <TextBlock Text="Products:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top" Margin="0,0,0,5"/>
                        <Border BorderThickness="1" BorderBrush="black" CornerRadius="20" 
        Background="White" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!-- Makes the TextBox fill the width -->
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0"
                 Height="35"
                 Margin="20,0,0,0"
                 VerticalAlignment="Center"
                 VerticalContentAlignment="Center"
                 BorderThickness="0"
                 FontFamily="Lexend"
                 Background="Transparent"
                 HorizontalAlignment="Stretch"
                 Focusable="True"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <StackPanel x:Name="PrintSettings" Grid.Row="1" Margin="25,0,25,30">
                <Border BorderThickness="0,2,0,0" BorderBrush="#AEAEAE" Margin="-10,0,-10,25" Width="Auto" HorizontalAlignment="Stretch"/>

                <TextBlock Text="Print Settings" FontWeight="Regular" FontFamily="Lexend" FontSize="20" Foreground="#474747" VerticalAlignment="Top" Margin="0,0,0,20"/>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".25*"/>
                        <ColumnDefinition Width=".25*"/>
                        <ColumnDefinition Width=".25*"/>
                        <ColumnDefinition Width=".25*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0">
                        <TextBlock Text="Barcode Type:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top" Margin="0,0,0,5"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="0"  Margin="10,0,25,10">
                        <ComboBox SelectedIndex="0" FontFamily="Lexend" FontSize="14" Foreground="#474747"
    Padding="10" Width="Auto" Height="40" Background="White" BorderBrush="black"
    BorderThickness="1" HorizontalContentAlignment="Left">
                            <ComboBox.Resources>
                                <!-- Custom ComboBox Toggle Button -->
                                <Style x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
                                    <Setter Property="Focusable" Value="False"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent" Padding="5">
                                                    <Grid>
                                                        <Path x:Name="Arrow" Data="M 0 0 L 4 4 L 8 0 Z"
              HorizontalAlignment="Right" VerticalAlignment="Center"
              Fill="Black" Width="8" Height="8" 
              Margin="0,5,10,0"/>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>

                                <!-- Custom ComboBox Style -->
                                <Style TargetType="ComboBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBox">
                                                <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="20" Padding="5">
                                                    <Grid>
                                                        <!-- Toggle Button for Dropdown -->
                                                        <ToggleButton Name="ToggleButton" 
                                      Style="{StaticResource ComboBoxToggleButton}"
                                      Grid.Column="2" Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" 
                                      ClickMode="Press"/>

                                                        <!-- Selected Content -->
                                                        <ContentPresenter Name="ContentSite" IsHitTestVisible="False" 
                                      Content="{TemplateBinding SelectionBoxItem}" 
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      Margin="10,0,40,0" VerticalAlignment="Center"/>

                                                        <!-- Dropdown List -->
                                                        <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" 
                                       AllowsTransparency="True" Focusable="False"
                                       PopupAnimation="Slide"
                                                              
                                       MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                                            <Border Background="White" BorderBrush="Gray" BorderThickness="1" 
                                        CornerRadius="10"
                                        Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                                                <ScrollViewer>
                                                                    <StackPanel>
                                                                        <ItemsPresenter/>
                                                                    </StackPanel>
                                                                </ScrollViewer>
                                                            </Border>
                                                        </Popup>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.Resources>

                            <!-- ComboBox Items -->
                            <ComboBoxItem Content="EAN-13"/>
                            <ComboBoxItem Content="CODE-128"/>
                            <ComboBoxItem Content="CODE-39"/>
                            <ComboBoxItem Content="EAN-5"/>
                            <ComboBoxItem Content="EAN-8"/>
                            <ComboBoxItem Content="UPC-A"/>
                            <ComboBoxItem Content="UPC-E"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1">
                        <TextBlock Text="Barcode Width:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top" Margin="0,0,0,5"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,25,10">
                        <Border BorderThickness="1" BorderBrush="black" CornerRadius="20" 
        Background="White" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!-- Makes the TextBox fill the width -->
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0"
                 Height="35"
                 Margin="20,0,0,0"
                 VerticalAlignment="Center"
                 VerticalContentAlignment="Center"
                 BorderThickness="0"
                 FontFamily="Lexend"
                 Background="Transparent"
                 HorizontalAlignment="Stretch"
                 Focusable="True"/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="2">
                        <TextBlock Text="Barcode Height:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top" Margin="0,0,0,5"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="2" Margin="10,0,25,10">
                        <Border BorderThickness="1" BorderBrush="black" CornerRadius="20" 
Background="White" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!-- Makes the TextBox fill the width -->
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0"
         Height="35"
         Margin="20,0,0,0"
         VerticalAlignment="Center"
         VerticalContentAlignment="Center"
         BorderThickness="0"
         FontFamily="Lexend"
         Background="Transparent"
         HorizontalAlignment="Stretch"
         Focusable="True"/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="3">
                        <TextBlock Text="Font Size:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top" Margin="0,0,0,5"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="3"  Margin="10,0,25,10">
                        <ComboBox SelectedIndex="3" FontFamily="Lexend" FontSize="14" Foreground="#474747"
Padding="10" Width="Auto" Height="40" Background="White" BorderBrush="black"
BorderThickness="1" HorizontalContentAlignment="Left">
                            <ComboBox.Resources>
                                <!-- Custom ComboBox Toggle Button -->
                                <Style x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
                                    <Setter Property="Focusable" Value="False"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent" Padding="5">
                                                    <Grid>
                                                        <Path x:Name="Arrow" Data="M 0 0 L 4 4 L 8 0 Z"
          HorizontalAlignment="Right" VerticalAlignment="Center"
          Fill="Black" Width="8" Height="8" 
          Margin="0,5,10,0"/>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>

                                <!-- Custom ComboBox Style -->
                                <Style TargetType="ComboBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBox">
                                                <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="20" Padding="5">
                                                    <Grid>
                                                        <!-- Toggle Button for Dropdown -->
                                                        <ToggleButton Name="ToggleButton" 
                                  Style="{StaticResource ComboBoxToggleButton}"
                                  Grid.Column="2" Focusable="False"
                                  IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" 
                                  ClickMode="Press"/>

                                                        <!-- Selected Content -->
                                                        <ContentPresenter Name="ContentSite" IsHitTestVisible="False" 
                                  Content="{TemplateBinding SelectionBoxItem}" 
                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                  Margin="10,0,40,0" VerticalAlignment="Center"/>

                                                        <!-- Dropdown List -->
                                                        <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Slide"
                                                          
                                   MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                                            <Border Background="White" BorderBrush="Gray" BorderThickness="1" 
                                    CornerRadius="10"
                                    Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                                                <ScrollViewer>
                                                                    <StackPanel>
                                                                        <ItemsPresenter/>
                                                                    </StackPanel>
                                                                </ScrollViewer>
                                                            </Border>
                                                        </Popup>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.Resources>

                            <!-- ComboBox Items -->
                            <ComboBoxItem Content="8pt"/>
                            <ComboBoxItem Content="9pt"/>
                            <ComboBoxItem Content="10pt"/>
                            <ComboBoxItem Content="11pt"/>
                            <ComboBoxItem Content="12pt"/>
                            <ComboBoxItem Content="13pt"/>
                            <ComboBoxItem Content="14pt"/>
                            <ComboBoxItem Content="15pt"/>
                            <ComboBoxItem Content="16pt"/>
                            <ComboBoxItem Content="17pt"/>
                            <ComboBoxItem Content="18pt"/>
                            <ComboBoxItem Content="19pt"/>
                            <ComboBoxItem Content="20pt"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <Border BorderThickness="0,2,0,0" BorderBrush="#AEAEAE" Margin="-10,10,-10,15" Width="Auto" HorizontalAlignment="Stretch"/>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height=".5*"/>
                        <RowDefinition Height=".5*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".5*"/>
                        <ColumnDefinition Width=".5*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0">
                        <TextBlock Text="Label Width:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top" Margin="0,0,0,5"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="10,0,25,10">
                        <Border BorderThickness="1" BorderBrush="black" CornerRadius="20" 
Background="White" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!-- Makes the TextBox fill the width -->
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0"
         Height="35"
         Margin="20,0,0,0"
         VerticalAlignment="Center"
         VerticalContentAlignment="Center"
         BorderThickness="0"
         FontFamily="Lexend"
         Background="Transparent"
         HorizontalAlignment="Stretch"
         Focusable="True"/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1">
                        <TextBlock Text="Label Height:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top" Margin="0,0,0,5"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,25,10">
                        <Border BorderThickness="1" BorderBrush="black" CornerRadius="20" 
Background="White" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!-- Makes the TextBox fill the width -->
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0"
         Height="35"
         Margin="20,0,0,0"
         VerticalAlignment="Center"
         VerticalContentAlignment="Center"
         BorderThickness="0"
         FontFamily="Lexend"
         Background="Transparent"
         HorizontalAlignment="Stretch"
         Focusable="True"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>

                <Border BorderThickness="0,2,0,0" BorderBrush="#AEAEAE" Margin="-10,10,-10,15" Width="Auto" HorizontalAlignment="Stretch"/>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height=".5*"/>
                        <RowDefinition Height=".5*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".5*"/>
                        <ColumnDefinition Width=".5*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0">
                        <TextBlock Text="Sheet Width:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top" Margin="0,0,0,5"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="10,0,25,10">
                        <Border BorderThickness="1" BorderBrush="black" CornerRadius="20" 
Background="White" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!-- Makes the TextBox fill the width -->
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0"
         Height="35"
         Margin="20,0,0,0"
         VerticalAlignment="Center"
         VerticalContentAlignment="Center"
         BorderThickness="0"
         FontFamily="Lexend"
         Background="Transparent"
         HorizontalAlignment="Stretch"
         Focusable="True"/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1">
                        <TextBlock Text="Sheet Height:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top" Margin="0,0,0,5"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,25,10">
                        <Border BorderThickness="1" BorderBrush="black" CornerRadius="20" 
Background="White" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!-- Makes the TextBox fill the width -->
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0"
         Height="35"
         Margin="20,0,0,0"
         VerticalAlignment="Center"
         VerticalContentAlignment="Center"
         BorderThickness="0"
         FontFamily="Lexend"
         Background="Transparent"
         HorizontalAlignment="Stretch"
         Focusable="True"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>

                <Border BorderThickness="0,2,0,0" BorderBrush="#AEAEAE" Margin="-10,10,-10,15" Width="Auto" HorizontalAlignment="Stretch"/>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height=".5*"/>
                        <RowDefinition Height=".5*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".5*"/>
                        <ColumnDefinition Width=".5*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0">
                        <TextBlock Text="Total Rows:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top" Margin="0,0,0,5"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="10,0,25,10">
                        <ComboBox SelectedIndex="0" FontFamily="Lexend" FontSize="14" Foreground="#474747"
    Padding="10" Width="Auto" Height="40" Background="White" BorderBrush="black"
    BorderThickness="1" HorizontalContentAlignment="Left" Margin="0,0,0,10">
                            <ComboBox.Resources>
                                <!-- Custom ComboBox Toggle Button -->
                                <Style x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
                                    <Setter Property="Focusable" Value="False"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent" Padding="5">
                                                    <Grid>
                                                        <Path x:Name="Arrow" Data="M 0 0 L 4 4 L 8 0 Z"
              HorizontalAlignment="Right" VerticalAlignment="Center"
              Fill="Black" Width="8" Height="8" 
              Margin="0,5,10,0"/>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>

                                <!-- Custom ComboBox Style -->
                                <Style TargetType="ComboBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBox">
                                                <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="20" Padding="5">
                                                    <Grid>
                                                        <!-- Toggle Button for Dropdown -->
                                                        <ToggleButton Name="ToggleButton" 
                                      Style="{StaticResource ComboBoxToggleButton}"
                                      Grid.Column="2" Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" 
                                      ClickMode="Press"/>

                                                        <!-- Selected Content -->
                                                        <ContentPresenter Name="ContentSite" IsHitTestVisible="False" 
                                      Content="{TemplateBinding SelectionBoxItem}" 
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      Margin="10,0,40,0" VerticalAlignment="Center"/>

                                                        <!-- Dropdown List -->
                                                        <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" 
                                       AllowsTransparency="True" Focusable="False"
                                       PopupAnimation="Slide"
                                       MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                                            <Border Background="White" BorderBrush="Gray" BorderThickness="1" 
                                        CornerRadius="10"
                                        Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                                                <ScrollViewer>
                                                                    <StackPanel>
                                                                        <ItemsPresenter/>
                                                                    </StackPanel>
                                                                </ScrollViewer>
                                                            </Border>
                                                        </Popup>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.Resources>

                            <!-- ComboBox Items -->
                            <ComboBoxItem Content="1"/>
                            <ComboBoxItem Content="2"/>
                            <ComboBoxItem Content="3"/>
                            <ComboBoxItem Content="4"/>
                            <ComboBoxItem Content="5"/>
                            <ComboBoxItem Content="6"/>
                            <ComboBoxItem Content="7"/>
                            <ComboBoxItem Content="8"/>
                            <ComboBoxItem Content="9"/>
                            <ComboBoxItem Content="10"/>
                            <ComboBoxItem Content="20"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1">
                        <TextBlock Text="Total Columns:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top" Margin="0,0,0,5"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,25,10">
                        <ComboBox SelectedIndex="0" FontFamily="Lexend" FontSize="14" Foreground="#474747"
    Padding="10" Width="Auto" Height="40" Background="White" BorderBrush="black"
    BorderThickness="1" HorizontalContentAlignment="Left" Margin="0,0,0,10">
                            <ComboBox.Resources>
                                <!-- Custom ComboBox Toggle Button -->
                                <Style x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
                                    <Setter Property="Focusable" Value="False"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent" Padding="5">
                                                    <Grid>
                                                        <Path x:Name="Arrow" Data="M 0 0 L 4 4 L 8 0 Z"
              HorizontalAlignment="Right" VerticalAlignment="Center"
              Fill="Black" Width="8" Height="8" 
              Margin="0,5,10,0"/>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>

                                <!-- Custom ComboBox Style -->
                                <Style TargetType="ComboBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBox">
                                                <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="20" Padding="5">
                                                    <Grid>
                                                        <!-- Toggle Button for Dropdown -->
                                                        <ToggleButton Name="ToggleButton" 
                                      Style="{StaticResource ComboBoxToggleButton}"
                                      Grid.Column="2" Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" 
                                      ClickMode="Press"/>

                                                        <!-- Selected Content -->
                                                        <ContentPresenter Name="ContentSite" IsHitTestVisible="False" 
                                      Content="{TemplateBinding SelectionBoxItem}" 
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      Margin="10,0,40,0" VerticalAlignment="Center"/>

                                                        <!-- Dropdown List -->
                                                        <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" 
                                       AllowsTransparency="True" Focusable="False"
                                       PopupAnimation="Slide"
                                       MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                                            <Border Background="White" BorderBrush="Gray" BorderThickness="1" 
                                        CornerRadius="10"
                                        Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                                                <ScrollViewer>
                                                                    <StackPanel>
                                                                        <ItemsPresenter/>
                                                                    </StackPanel>
                                                                </ScrollViewer>
                                                            </Border>
                                                        </Popup>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.Resources>

                            <!-- ComboBox Items -->
                            <ComboBoxItem Content="1"/>
                            <ComboBoxItem Content="2"/>
                            <ComboBoxItem Content="3"/>
                            <ComboBoxItem Content="4"/>
                            <ComboBoxItem Content="5"/>
                            <ComboBoxItem Content="6"/>
                            <ComboBoxItem Content="7"/>
                            <ComboBoxItem Content="8"/>
                            <ComboBoxItem Content="9"/>
                            <ComboBoxItem Content="10"/>
                            <ComboBoxItem Content="20"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <Border BorderThickness="0,2,0,0" BorderBrush="#AEAEAE" Margin="-10,10,-10,15" Width="Auto" HorizontalAlignment="Stretch"/>

            </StackPanel>

            <StackPanel x:Name="PrintDetail" Grid.Row="2" Margin="25,-20,25,0">
                <StackPanel Margin="0,0,0,25">
                    <TextBlock Text="Print Details" FontWeight="Regular" FontFamily="Lexend" FontSize="20" Foreground="#474747" VerticalAlignment="Top"/>
                </StackPanel>
                
                <Grid Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".5*"/>
                        <ColumnDefinition Width=".5*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,10">
                        <TextBlock Text="Product Name:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,25,10">
                        <Border BorderThickness="2" CornerRadius="20" Padding="5" Width="Auto" Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <!-- Default Border Color -->
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Style.Triggers>
                                        <!-- Change to DarkGreen when Checked -->
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=AddProductName}" Value="True">
                                            <Setter Property="BorderBrush" Value="#456B2E"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- RadioButton with Custom Styling -->
                                <RadioButton x:Name="AddProductName" GroupName="ProductName" FontFamily="Lexend" FontSize="14" IsChecked="True">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <!-- Custom Outer Circle -->
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderThickness="2" Margin="5"
                                            x:Name="OuterCircle" BorderBrush="#474747">
                                                                <!-- Custom Inner Circle -->
                                                                <Ellipse Width="8" Height="8" Fill="#456B2E" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <TextBlock Text="Add" VerticalAlignment="Center" Foreground="#474747" x:Name="Label"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                                <Setter TargetName="Label" Property="Foreground" Value="#456B2E"/>
                                                                <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#456B2E"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>

                                <!-- Add Icon -->
                                <materialDesign:PackIcon Kind="PlusBoxOutline" Width="20" Height="20" Grid.Column="1" HorizontalAlignment="Right"
                                 VerticalAlignment="Center" Margin="10,0,10,0">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <!-- Default Icon Color -->
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, ElementName=AddProductName}" Value="True">
                                                    <Setter Property="Foreground" Value="DarkGreen"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,25,10">
                        <Border BorderThickness="2" CornerRadius="20" Padding="5" Width="Auto" Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <!-- Default Border Color -->
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Style.Triggers>
                                        <!-- Change to Red when Checked -->
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=RemoveProductName}" Value="True">
                                            <Setter Property="BorderBrush" Value="#D23636"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- RadioButton with Custom Styling -->
                                <RadioButton x:Name="RemoveProductName" GroupName="ProductName" FontFamily="Lexend" FontSize="14">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <!-- Custom Radio Circle -->
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderThickness="2" Margin="5" 
                                            x:Name="OuterCircle" BorderBrush="#474747">
                                                                <!-- Added Default Color -->
                                                                <Ellipse Width="8" Height="8" Fill="#D23636" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <TextBlock Text="Remove" VerticalAlignment="Center" Foreground="#474747" x:Name="Label"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                                <Setter TargetName="Label" Property="Foreground" Value="#D23636"/>
                                                                <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#D23636"/>
                                                                <!-- Outer Circle Turns Red -->
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>

                                <!-- Minus Icon -->
                                <materialDesign:PackIcon Kind="MinusBoxOutline" Width="20" Height="20" Grid.Column="1" HorizontalAlignment="Right"
         VerticalAlignment="Center" Margin="10,0,10,0">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, ElementName=RemoveProductName}" Value="True">
                                                    <Setter Property="Foreground" Value="#D23636"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".5*"/>
                        <ColumnDefinition Width=".5*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,10">
                        <TextBlock Text="Business Location:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,25,10">
                        <Border BorderThickness="2" CornerRadius="20" Padding="5" Width="Auto" Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <!-- Default Border Color -->
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Style.Triggers>
                                        <!-- Change to DarkGreen when Checked -->
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=AddBusinessLocation}" Value="True">
                                            <Setter Property="BorderBrush" Value="#456B2E"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- RadioButton with Custom Styling -->
                                <RadioButton x:Name="AddBusinessLocation" GroupName="BusinessLocation" FontFamily="Lexend" FontSize="14" IsChecked="True">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <!-- Custom Outer Circle -->
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderThickness="2" Margin="5"
                                   x:Name="OuterCircle" BorderBrush="#474747">
                                                                <!-- Custom Inner Circle -->
                                                                <Ellipse Width="8" Height="8" Fill="#456B2E" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <TextBlock Text="Add" VerticalAlignment="Center" Foreground="#474747" x:Name="Label"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                                <Setter TargetName="Label" Property="Foreground" Value="#456B2E"/>
                                                                <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#456B2E"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>

                                <!-- Add Icon -->
                                <materialDesign:PackIcon Kind="PlusBoxOutline" Width="20" Height="20" Grid.Column="1" HorizontalAlignment="Right"
                        VerticalAlignment="Center" Margin="10,0,10,0">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <!-- Default Icon Color -->
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, ElementName=AddBusinessLocation}" Value="True">
                                                    <Setter Property="Foreground" Value="#456B2E"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,25,10">
                        <Border BorderThickness="2" CornerRadius="20" Padding="5" Width="Auto" Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <!-- Default Border Color -->
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Style.Triggers>
                                        <!-- Change to Red when Checked -->
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=RemoveBusinessLocation}" Value="True">
                                            <Setter Property="BorderBrush" Value="#D23636"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- RadioButton with Custom Styling -->
                                <RadioButton x:Name="RemoveBusinessLocation" GroupName="BusinessLocation" FontFamily="Lexend" FontSize="14">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <!-- Custom Radio Circle -->
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderThickness="2" Margin="5" 
                                   x:Name="OuterCircle" BorderBrush="#474747">
                                                                <!-- Added Default Color -->
                                                                <Ellipse Width="8" Height="8" Fill="#D23636" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <TextBlock Text="Remove" VerticalAlignment="Center" Foreground="#474747" x:Name="Label"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                                <Setter TargetName="Label" Property="Foreground" Value="#D23636"/>
                                                                <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#D23636"/>
                                                                <!-- Outer Circle Turns Red -->
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>

                                <!-- Minus Icon -->
                                <materialDesign:PackIcon Kind="MinusBoxOutline" Width="20" Height="20" Grid.Column="1" HorizontalAlignment="Right"
VerticalAlignment="Center" Margin="10,0,10,0">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, ElementName=RemoveBusinessLocation}" Value="True">
                                                    <Setter Property="Foreground" Value="#D23636"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".5*"/>
                        <ColumnDefinition Width=".5*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,10">
                        <TextBlock Text="Warehouse:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,25,10">
                        <Border BorderThickness="2" CornerRadius="20" Padding="5" Width="Auto" Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <!-- Default Border Color -->
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Style.Triggers>
                                        <!-- Change to DarkGreen when Checked -->
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=AddWarehouse}" Value="True">
                                            <Setter Property="BorderBrush" Value="#456B2E"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- RadioButton with Custom Styling -->
                                <RadioButton x:Name="AddWarehouse" GroupName="Warehouse" FontFamily="Lexend" FontSize="14" IsChecked="True">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <!-- Custom Outer Circle -->
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderThickness="2" Margin="5"
                                   x:Name="OuterCircle" BorderBrush="#474747">
                                                                <!-- Custom Inner Circle -->
                                                                <Ellipse Width="8" Height="8" Fill="#456B2E" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <TextBlock Text="Add" VerticalAlignment="Center" Foreground="#474747" x:Name="Label"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                                <Setter TargetName="Label" Property="Foreground" Value="#456B2E"/>
                                                                <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#456B2E"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>

                                <!-- Add Icon -->
                                <materialDesign:PackIcon Kind="PlusBoxOutline" Width="20" Height="20" Grid.Column="1" HorizontalAlignment="Right"
                        VerticalAlignment="Center" Margin="10,0,10,0">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <!-- Default Icon Color -->
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, ElementName=AddWarehouse}" Value="True">
                                                    <Setter Property="Foreground" Value="#456B2E"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,25,10">
                        <Border BorderThickness="2" CornerRadius="20" Padding="5" Width="Auto" Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <!-- Default Border Color -->
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Style.Triggers>
                                        <!-- Change to Red when Checked -->
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=RemoveWarehouse}" Value="True">
                                            <Setter Property="BorderBrush" Value="#D23636"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- RadioButton with Custom Styling -->
                                <RadioButton x:Name="RemoveWarehouse" GroupName="Warehouse" FontFamily="Lexend" FontSize="14">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <!-- Custom Radio Circle -->
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderThickness="2" Margin="5" 
                                   x:Name="OuterCircle" BorderBrush="#474747">
                                                                <!-- Added Default Color -->
                                                                <Ellipse Width="8" Height="8" Fill="#D23636" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <TextBlock Text="Remove" VerticalAlignment="Center" Foreground="#474747" x:Name="Label"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                                <Setter TargetName="Label" Property="Foreground" Value="#D23636"/>
                                                                <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#D23636"/>
                                                                <!-- Outer Circle Turns Red -->
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>

                                <!-- Minus Icon -->
                                <materialDesign:PackIcon Kind="MinusBoxOutline" Width="20" Height="20" Grid.Column="1" HorizontalAlignment="Right"
VerticalAlignment="Center" Margin="10,0,10,0">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, ElementName=RemoveWarehouse}" Value="True">
                                                    <Setter Property="Foreground" Value="#D23636"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".5*"/>
                        <ColumnDefinition Width=".5*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,10">
                        <TextBlock Text="Price:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,25,10">
                        <Border BorderThickness="2" CornerRadius="20" Padding="5" Width="Auto" Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <!-- Default Border Color -->
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Style.Triggers>
                                        <!-- Change to DarkGreen when Checked -->
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=AddPrice}" Value="True">
                                            <Setter Property="BorderBrush" Value="#456B2E"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- RadioButton with Custom Styling -->
                                <RadioButton x:Name="AddPrice" GroupName="Price" FontFamily="Lexend" FontSize="14" IsChecked="True">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <!-- Custom Outer Circle -->
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderThickness="2" Margin="5"
                                   x:Name="OuterCircle" BorderBrush="#474747">
                                                                <!-- Custom Inner Circle -->
                                                                <Ellipse Width="8" Height="8" Fill="#456B2E" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <TextBlock Text="Add" VerticalAlignment="Center" Foreground="#474747" x:Name="Label"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                                <Setter TargetName="Label" Property="Foreground" Value="#456B2E"/>
                                                                <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#456B2E"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>

                                <!-- Add Icon -->
                                <materialDesign:PackIcon Kind="PlusBoxOutline" Width="20" Height="20" Grid.Column="1" HorizontalAlignment="Right"
                        VerticalAlignment="Center" Margin="10,0,10,0">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <!-- Default Icon Color -->
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, ElementName=AddPrice}" Value="True">
                                                    <Setter Property="Foreground" Value="#456B2E"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,25,10">
                        <Border BorderThickness="2" CornerRadius="20" Padding="5" Width="Auto" Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <!-- Default Border Color -->
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Style.Triggers>
                                        <!-- Change to Red when Checked -->
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=RemovePrice}" Value="True">
                                            <Setter Property="BorderBrush" Value="#D23636"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- RadioButton with Custom Styling -->
                                <RadioButton x:Name="RemovePrice" GroupName="Price" FontFamily="Lexend" FontSize="14">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <!-- Custom Radio Circle -->
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderThickness="2" Margin="5" 
                                   x:Name="OuterCircle" BorderBrush="#474747">
                                                                <!-- Added Default Color -->
                                                                <Ellipse Width="8" Height="8" Fill="#D23636" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <TextBlock Text="Remove" VerticalAlignment="Center" Foreground="#474747" x:Name="Label"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                                <Setter TargetName="Label" Property="Foreground" Value="#D23636"/>
                                                                <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#D23636"/>
                                                                <!-- Outer Circle Turns Red -->
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>

                                <!-- Minus Icon -->
                                <materialDesign:PackIcon Kind="MinusBoxOutline" Width="20" Height="20" Grid.Column="1" HorizontalAlignment="Right"
VerticalAlignment="Center" Margin="10,0,10,0">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, ElementName=RemovePrice}" Value="True">
                                                    <Setter Property="Foreground" Value="#D23636"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".5*"/>
                        <ColumnDefinition Width=".5*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,10">
                        <TextBlock Text="Product Code:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,25,10">
                        <Border BorderThickness="2" CornerRadius="20" Padding="5" Width="Auto" Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <!-- Default Border Color -->
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Style.Triggers>
                                        <!-- Change to DarkGreen when Checked -->
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=AddProductCode}" Value="True">
                                            <Setter Property="BorderBrush" Value="#456B2E"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- RadioButton with Custom Styling -->
                                <RadioButton x:Name="AddProductCode" GroupName="ProductCode" FontFamily="Lexend" FontSize="14" IsChecked="True">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <!-- Custom Outer Circle -->
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderThickness="2" Margin="5"
                                   x:Name="OuterCircle" BorderBrush="#474747">
                                                                <!-- Custom Inner Circle -->
                                                                <Ellipse Width="8" Height="8" Fill="#456B2E" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <TextBlock Text="Add" VerticalAlignment="Center" Foreground="#474747" x:Name="Label"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                                <Setter TargetName="Label" Property="Foreground" Value="#456B2E"/>
                                                                <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#456B2E"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>

                                <!-- Add Icon -->
                                <materialDesign:PackIcon Kind="PlusBoxOutline" Width="20" Height="20" Grid.Column="1" HorizontalAlignment="Right"
                        VerticalAlignment="Center" Margin="10,0,10,0">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <!-- Default Icon Color -->
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, ElementName=AddProductCode}" Value="True">
                                                    <Setter Property="Foreground" Value="#456B2E"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,25,10">
                        <Border BorderThickness="2" CornerRadius="20" Padding="5" Width="Auto" Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <!-- Default Border Color -->
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Style.Triggers>
                                        <!-- Change to Red when Checked -->
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=RemoveProductCode}" Value="True">
                                            <Setter Property="BorderBrush" Value="#D23636"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- RadioButton with Custom Styling -->
                                <RadioButton x:Name="RemoveProductCode" GroupName="ProductCode" FontFamily="Lexend" FontSize="14">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <!-- Custom Radio Circle -->
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderThickness="2" Margin="5" 
                                   x:Name="OuterCircle" BorderBrush="#474747">
                                                                <!-- Added Default Color -->
                                                                <Ellipse Width="8" Height="8" Fill="#D23636" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <TextBlock Text="Remove" VerticalAlignment="Center" Foreground="#474747" x:Name="Label"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                                <Setter TargetName="Label" Property="Foreground" Value="#D23636"/>
                                                                <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#D23636"/>
                                                                <!-- Outer Circle Turns Red -->
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>

                                <!-- Minus Icon -->
                                <materialDesign:PackIcon Kind="MinusBoxOutline" Width="20" Height="20" Grid.Column="1" HorizontalAlignment="Right"
VerticalAlignment="Center" Margin="10,0,10,0">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, ElementName=RemoveProductCode}" Value="True">
                                                    <Setter Property="Foreground" Value="#D23636"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".5*"/>
                        <ColumnDefinition Width=".5*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,10">
                        <TextBlock Text="Products:" FontWeight="Regular" FontFamily="Lexend" FontSize="14" Foreground="#474747" VerticalAlignment="Top"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,25,10">
                        <Border BorderThickness="2" CornerRadius="20" Padding="5" Width="Auto" Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <!-- Default Border Color -->
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Style.Triggers>
                                        <!-- Change to DarkGreen when Checked -->
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=AddProducts}" Value="True">
                                            <Setter Property="BorderBrush" Value="#456B2E"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- RadioButton with Custom Styling -->
                                <RadioButton x:Name="AddProducts" GroupName="Products" FontFamily="Lexend" FontSize="14" IsChecked="True">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <!-- Custom Outer Circle -->
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderThickness="2" Margin="5"
                                   x:Name="OuterCircle" BorderBrush="#474747">
                                                                <!-- Custom Inner Circle -->
                                                                <Ellipse Width="8" Height="8" Fill="#456B2E" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <TextBlock Text="Add" VerticalAlignment="Center" Foreground="#474747" x:Name="Label"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                                <Setter TargetName="Label" Property="Foreground" Value="#456B2E"/>
                                                                <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#456B2E"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>

                                <!-- Add Icon -->
                                <materialDesign:PackIcon Kind="PlusBoxOutline" Width="20" Height="20" Grid.Column="1" HorizontalAlignment="Right"
                        VerticalAlignment="Center" Margin="10,0,10,0">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <!-- Default Icon Color -->
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, ElementName=AddProducts}" Value="True">
                                                    <Setter Property="Foreground" Value="#456B2E"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,25,10">
                        <Border BorderThickness="2" CornerRadius="20" Padding="5" Width="Auto" Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <!-- Default Border Color -->
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Style.Triggers>
                                        <!-- Change to Red when Checked -->
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=RemoveProducts}" Value="True">
                                            <Setter Property="BorderBrush" Value="#D23636"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- RadioButton with Custom Styling -->
                                <RadioButton x:Name="RemoveProducts" GroupName="Products" FontFamily="Lexend" FontSize="14">
                                    <RadioButton.Style>
                                        <Style TargetType="RadioButton">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RadioButton">
                                                        <StackPanel Orientation="Horizontal">
                                                            <!-- Custom Radio Circle -->
                                                            <Border Width="16" Height="16" CornerRadius="8" BorderThickness="2" Margin="5" 
                                   x:Name="OuterCircle" BorderBrush="#474747">
                                                                <!-- Added Default Color -->
                                                                <Ellipse Width="8" Height="8" Fill="#D23636" Visibility="Collapsed" x:Name="Dot"/>
                                                            </Border>
                                                            <TextBlock Text="Remove" VerticalAlignment="Center" Foreground="#474747" x:Name="Label"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                                                <Setter TargetName="Label" Property="Foreground" Value="#D23636"/>
                                                                <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#D23636"/>
                                                                <!-- Outer Circle Turns Red -->
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RadioButton.Style>
                                </RadioButton>

                                <!-- Minus Icon -->
                                <materialDesign:PackIcon Kind="MinusBoxOutline" Width="20" Height="20" Grid.Column="1" HorizontalAlignment="Right"
VerticalAlignment="Center" Margin="10,0,10,0">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <Setter Property="Foreground" Value="#474747"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, ElementName=RemoveProducts}" Value="True">
                                                    <Setter Property="Foreground" Value="#D23636"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>

                <StackPanel Margin="25,25,25,40" HorizontalAlignment="Right">
                    <Button Width="100" Height="40" Background="#244270" Foreground="White"
        Padding="10" HorizontalAlignment="Left">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="20" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="PrinterPosPlus" Width="20" Height="20" Foreground="White" Margin="0,0,5,0"/>
                            <TextBlock Text="Print" FontSize="14" VerticalAlignment="Center" FontWeight="Bold"/>
                        </StackPanel>
                    </Button>

                </StackPanel>
            </StackPanel>
        </Grid>
    </ScrollViewer>

</Window>