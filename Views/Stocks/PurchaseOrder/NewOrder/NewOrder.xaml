<UserControl x:Class="DPC.Views.Stocks.PurchaseOrder.NewOrder.NewOrder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Background="#D9D9D9" FontFamily="Lexend" 
             d:DesignWidth="1100" d:DesignHeight="900"
             HorizontalAlignment="Stretch">

    <UserControl.Resources>
        <Style x:Key="RoundedBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#AEAEAE"/>
        </Style>
        <Style x:Key="RoundedTextboxStyle" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="ModernDataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1D3242"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="Padding" Value="5,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#2D4252"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="25,20,25,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <materialDesign:PackIcon Kind="CartOutline" Width="30" Height="30" Foreground="#474747" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock x:Name="lblPageTitle" Text="Purchase Order" FontWeight="SemiBold" FontSize="25" Foreground="#474747" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnAddSupplier" Click="BtnAddSupplier_Click" Style="{StaticResource MaterialDesignFlatButton}" Foreground="#1D3242">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountPlus" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="New Supplier" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button x:Name="BtnReset" Click="BtnReset_Click" Style="{StaticResource MaterialDesignFlatButton}" Foreground="#D23636" Margin="10,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Reload" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Reset Form" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,15,0">
                    <TextBlock Text="Search Supplier:" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#474747"/>
                    <Border Style="{StaticResource RoundedBorderStyle}" Background="White" Height="40">
                        <Grid>
                            <TextBox x:Name="txtSearchSupplier" TextChanged="txtSearchSupplier_TextChanged" Style="{StaticResource RoundedTextboxStyle}" Padding="10,0"/>
                            <materialDesign:PackIcon Kind="Magnify" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#AEAEAE"/>
                        </Grid>
                    </Border>
                    <Popup x:Name="AutoCompletePopup" PlacementTarget="{Binding ElementName=txtSearchSupplier}" Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
                        <Border Background="White" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5">
                            <ListBox x:Name="LstItems" SelectionChanged="LstItems_SelectionChanged" MaxHeight="150" Width="{Binding ElementName=txtSearchSupplier, Path=ActualWidth}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding SupplierName}" Padding="8"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Border>
                    </Popup>

                    <TextBlock Text="Supplier Details:" FontWeight="SemiBold" Margin="0,15,0,5" Foreground="#474747"/>
                    <Border Style="{StaticResource RoundedBorderStyle}" Background="White">
                        <TextBox x:Name="TxtSupplierDetails" Height="150" TextWrapping="Wrap" AcceptsReturn="True" Style="{StaticResource RoundedTextboxStyle}" Padding="10" IsReadOnly="True" VerticalContentAlignment="Top"/>
                    </Border>

                    <TextBlock Text="Warehouse Source:" FontWeight="SemiBold" Margin="0,15,0,5" Foreground="#474747"/>
                    <ComboBox x:Name="ComboBoxWarehouse" SelectionChanged="ComboBoxWarehouse_SelectionChanged" Style="{StaticResource RoundedComboBoxStyle}" Height="40" Background="White"/>
                </StackPanel>

                <Border Grid.Column="1" Background="White" CornerRadius="15" Padding="20" BorderBrush="#AEAEAE" BorderThickness="1" Margin="15,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Order Properties" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#1D3242"/>

                        <TextBlock Text="Invoice Number:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <Border Style="{StaticResource RoundedBorderStyle}" Height="35" Margin="0,0,0,10" Background="#F5F5F5">
                            <TextBox x:Name="txtInvoiceNumber" IsReadOnly="True" Style="{StaticResource RoundedTextboxStyle}" Padding="10,0" FontWeight="Bold"/>
                        </Border>

                        <TextBlock Text="Order Date:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <Border Style="{StaticResource RoundedBorderStyle}" Height="35" Margin="0,0,0,10">
                            <Grid>
                                <DatePicker x:Name="OrderDatePicker" Visibility="Hidden"/>
                                <Button x:Name="OrderDateButton" Click="OrderDateButton_Click" Style="{StaticResource DateButtonStyle}" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch" Padding="10,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding FormattedDate}" VerticalAlignment="Center" Grid.Column="0"/>
                                        <materialDesign:PackIcon Kind="CalendarMonth" Grid.Column="1" Foreground="#474747"/>
                                    </Grid>
                                </Button>
                            </Grid>
                        </Border>

                        <TextBlock Text="Due Date:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <Border Style="{StaticResource RoundedBorderStyle}" Height="35" Margin="0,0,0,10">
                            <Grid>
                                <DatePicker x:Name="OrderDueDatePicker" Visibility="Hidden"/>
                                <Button x:Name="OrderDueDateButton" Click="OrderDueDateButton_Click" Style="{StaticResource DateButtonStyle}" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch" Padding="10,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding FormattedDate}" VerticalAlignment="Center" Grid.Column="0"/>
                                        <materialDesign:PackIcon Kind="CalendarMonth" Grid.Column="1" Foreground="#474747"/>
                                    </Grid>
                                </Button>
                            </Grid>
                        </Border>

                        <TextBlock Text="Order Note:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <Border Style="{StaticResource RoundedBorderStyle}" Height="115">
                            <TextBox x:Name="txtOrderNote" TextWrapping="Wrap" AcceptsReturn="True" BorderThickness="0" Background="Transparent" Padding="10" VerticalContentAlignment="Top"/>
                        </Border>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,25,0,0">
                    <DataGrid x:Name="HeaderDataGrid" Background="Transparent" BorderThickness="0" AutoGenerateColumns="False" 
                              CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="None"
                              ColumnHeaderStyle="{StaticResource ModernDataGridHeaderStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ITEM DESCRIPTION" Width="160"/>
                            <DataGridTextColumn Header="QTY" Width="60"/>
                            <DataGridTextColumn Header="UNIT PRICE" Width="95"/>
                            <DataGridTextColumn x:Name="TaxHeader" Header="TAX (%)" Width="75"/>
                            <DataGridTextColumn Header="TAX VALUE" Width="75"/>
                            <DataGridTextColumn Header="DISC (%)" Width="85"/>
                            <DataGridTextColumn Header="DISCOUNT" Width="85"/>
                            <DataGridTextColumn Header="TOTAL" Width="100"/>
                            <DataGridTextColumn Header="" Width="50"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <ScrollViewer MaxHeight="1070" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="MainContainer" Background="Transparent"/>
                    </ScrollViewer>

                    <Button Click="AddNewCategory_Click" HorizontalAlignment="Center" Margin="0,15,0,0" Style="{StaticResource MaterialDesignOutlinedButton}" BorderBrush="#1D3242" Foreground="#1D3242" Height="35">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CategoryPlusOutline" Margin="0,0,5,0"/>
                            <TextBlock Text="New Category"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <Grid Grid.Row="2" Grid.ColumnSpan="2" Margin="0,30,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Rectangle Grid.Row="0" Grid.ColumnSpan="2" Height="1" Fill="#AEAEAE" Margin="0,0,0,25" HorizontalAlignment="Stretch"/>

                    <StackPanel Grid.Column="0" Grid.Row="1">
                        <TextBlock Text="Calculation Settings" FontWeight="Bold" Foreground="#1D3242" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Delivery Fee" Width="120" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <Border Width="180" Height="40" Style="{StaticResource RoundedBorderStyle}" BorderThickness="2" BorderBrush="Black" Background="#F5F5F5" CornerRadius="20">
                                <TextBox x:Name="txtDeliveryFee" TextChanged="txtDeliveryFee_TextChange" Style="{StaticResource RoundedTextboxStyle}" FontWeight="Bold"/>
                            </Border>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Tax Mode:" Width="120" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <ComboBox x:Name="txtTaxSelection" SelectionChanged="txtTaxSelection_SelectionChanged" Width="180" Style="{StaticResource RoundedComboBoxStyle}">
                                <ComboBoxItem Content="Inclusive" IsSelected="True"/>
                                <ComboBoxItem Content="Exclusive"/>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Discount Mode:" Width="120" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <ComboBox x:Name="txtDiscountSelection" Width="220" Style="{StaticResource RoundedComboBoxStyle}">
                                <ComboBoxItem Content="% Discount After" IsSelected="True"/>
                                <ComboBoxItem Content="Flat Discount After"/>
                                <ComboBoxItem Content="% Discount Before"/>
                                <ComboBoxItem Content="Flat Discount Before"/>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Grid.Row="1">
                        <Border Background="White" CornerRadius="15" Padding="20" BorderBrush="#AEAEAE" BorderThickness="1">
                            <StackPanel>
                                <Grid Margin="0,0,0,8">
                                    <TextBlock Text="Delivery Fee" Foreground="#555555"/>
                                    <TextBlock x:Name="lblFee" Text="₱ 0.00" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#D23636"/>
                                </Grid>
                                <Grid Margin="0,0,0,8">
                                    <TextBlock Text="Total Tax" Foreground="#555555"/>
                                    <TextBlock x:Name="txtTotalTax" Text="₱ 0.00" HorizontalAlignment="Right" FontWeight="Bold"/>
                                </Grid>
                                <Grid Margin="0,0,0,8">
                                    <TextBlock Text="Total Discount" Foreground="#555555"/>
                                    <TextBlock x:Name="txtTotalDiscount" Text="₱ 0.00" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#D23636"/>
                                </Grid>
                                <Separator Margin="0,10" Background="#AEAEAE"/>
                                <Grid>
                                    <TextBlock Text="GRAND TOTAL" FontSize="18" FontWeight="Bold" Foreground="#1D3242"/>
                                    <TextBlock x:Name="txtGrandTotal" Text="₱ 0.00" FontSize="24" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#1D5642"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Button x:Name="btnGenerateOrder" Click="btnGenerateOrder_Click" Margin="0,15,0,0" Height="55" Background="#1D5642" BorderBrush="#1D5642" Style="{StaticResource MaterialDesignRaisedButton}" materialDesign:ButtonAssist.CornerRadius="27">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileSend" Width="24" Height="24" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="White"/>
                                <TextBlock Text="GENERATE PURCHASE ORDER" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Foreground="White"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>