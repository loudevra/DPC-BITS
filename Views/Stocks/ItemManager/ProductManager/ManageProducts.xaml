<UserControl x:Class="DPC.Views.Stocks.ItemManager.ProductManager.ManageProducts"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="namespace:DPC.Views.Stocks.ItemManager.ProductManager.ManageProducts"
        mc:Ignorable="d"
        Background="#D9D9D9">

    <UserControl.Resources>
        <Style x:Key="AddNewButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="#D9D9D9"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#474747"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Border Style -->
        <Style x:Key="RoundedBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="15"/>
        </Style>

        <!-- Button Style -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Style="{StaticResource RoundedBorderStyle}" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedComboBoxStyle" TargetType="ComboBox">
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Style="{StaticResource RoundedBorderStyle}" 
                    Background="{TemplateBinding Background}" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="1">
                            <Grid>
                                <!-- Define two columns, Content on left, Arrow on right -->
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="35" />
                                </Grid.ColumnDefinitions>

                                <!-- Selected Item Content -->
                                <ContentPresenter Name="ContentSite"
                                      IsHitTestVisible="False"
                                      Content="{TemplateBinding SelectionBoxItem}"
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      VerticalAlignment="Center"
                                      Margin="10,0,5,0"/>

                                <!-- Arrow Button (Right) -->
                                <ToggleButton Name="ToggleButton" 
                                  Grid.Column="1"
                                  Focusable="False"
                                  IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                  Background="Transparent"
                                  BorderBrush="Transparent"
                                  BorderThickness="0"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center"
                                  Margin="0,0,5,0">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton" BasedOn="{StaticResource {x:Type ToggleButton}}">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="OverridesDefaultStyle" Value="True"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border Background="Transparent" BorderThickness="0">
                                                            <materialDesign:PackIcon Kind="ChevronDown" Width="20" Height="20" Foreground="Black"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>

                                <!-- Dropdown Items -->
                                <Popup Name="Popup"
                           Placement="Bottom"
                           IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                           AllowsTransparency="True"
                           Focusable="False"
                           MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <!-- MATCH WIDTH -->
                                    <Border Style="{StaticResource RoundedBorderStyle}" 
                                Background="White" 
                                BorderBrush="Black"
                                Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                        <!-- MATCH WIDTH -->
                                        <ScrollViewer>
                                            <StackPanel>
                                                <ItemsPresenter/>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedGridStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <!-- Added style for ModernScrollViewerStyle -->
        <Style x:Key="ModernScrollViewerStyle" TargetType="ScrollViewer">
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>

        <!-- Added style for RoundedTextboxStyle -->
        <Style x:Key="RoundedTextboxStyle" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Custom DataGrid Style -->
        <Style x:Key="CustomDataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="CanUserResizeColumns" Value="True"/>
            <Setter Property="CanUserReorderColumns" Value="True"/>
            <Setter Property="ColumnWidth" Value="SizeToHeader"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        </Style>

        <!-- Centered Cell Style for text columns -->
        <Style x:Key="CenteredCellStyle" TargetType="DataGridCell">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" BorderThickness="0">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title and Add Button -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,10,0,20">
                <materialDesign:PackIcon Kind="PackageVariantClosedPlus" Width="60" Height="60" Foreground="#474747"/>
                <TextBlock Text="Manage Products"
                           FontWeight="SemiBold"
                           FontFamily="Lexend"
                           FontSize="25"
                           Foreground="#474747"
                           VerticalAlignment="Center"/>
                <Button x:Name="BtnAddNew"
                        Style="{StaticResource AddNewButtonStyle}"
                        Margin="20,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="PackageVariantClosedPlus" Width="25" Height="25" Foreground="#474747"/>
                        <TextBlock Text="Product" FontSize="17" Foreground="#474747"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Status Cards -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- In Stock Card -->
                <Border Grid.Column="0" CornerRadius="10" BorderThickness="2" BorderBrush="#456B2E" Background="White" Margin="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="30,15">
                            <TextBlock x:Name="txtInStock" Text="0" FontSize="32" FontWeight="Bold" Foreground="#456B2E" FontFamily="Lexend" Height="40"/>
                            <TextBlock Text="In Stock" FontSize="12" Foreground="Gray" FontFamily="Lexend"/>
                        </StackPanel>
                        <materialDesign:PackIcon Grid.Column="1" Kind="CubeOutline" Width="70" Height="60" Foreground="#456B2E" VerticalAlignment="Center" Margin="20" HorizontalAlignment="Right"/>
                    </Grid>
                </Border>

                <!-- Stock Out Card -->
                <Border Grid.Column="1" CornerRadius="10" BorderThickness="2" BorderBrush="#C64141" Background="White" Margin="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="30,15">
                            <TextBlock x:Name="txtStockOut" Text="0" FontSize="32" FontWeight="Bold" Foreground="#C64141" FontFamily="Lexend" />
                            <TextBlock Text="Out of Stock" FontSize="12" Foreground="Gray" FontFamily="Lexend" />
                        </StackPanel>
                        <materialDesign:PackIcon Grid.Column="1" Kind="PackageVariantRemove" Width="70" Height="60" Foreground="#C64141" VerticalAlignment="Center" Margin="20" HorizontalAlignment="Right"/>
                    </Grid>
                </Border>

                <!-- Total Card -->
                <Border Grid.Column="2" CornerRadius="10" BorderThickness="2" BorderBrush="#6B4FA1" Background="White" Margin="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="30,15">
                            <TextBlock x:Name="txtTotal" Text="0" FontSize="32" FontWeight="Bold" Foreground="#6B4FA1" FontFamily="Lexend" />
                            <TextBlock Text="Total" FontSize="12" Foreground="Gray"  FontFamily="Lexend" />
                        </StackPanel>
                        <materialDesign:PackIcon Grid.Column="1" Kind="ChartPieOutline" Width="70" Height="60" Foreground="#6B4FA1" VerticalAlignment="Center" Margin="20" HorizontalAlignment="Right"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- Filters and Controls -->
            <Border Grid.Row="2" Padding="5" Background="White" CornerRadius="10" BorderBrush="White" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left Controls -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <!-- Excel Button -->
                        <Button Style="{StaticResource RoundedButtonStyle}" Background="#456B2E" Foreground="White" Width="75" Click="ExportToExcel" Height="29" Margin="10,0,0,0">
                            <materialDesign:PackIcon Kind="MicrosoftExcel" Width="25" Height="25"/>
                        </Button>

                        <TextBlock Text="Show" FontSize="14" Margin="10,0,5,0" Height="18" FontWeight="Regular" FontFamily="Lexend" Foreground="#474747" VerticalAlignment="Center" />
                        <ComboBox x:Name="cmbPageSize" Width="75" Style="{StaticResource RoundedComboBoxStyle}" Height="28">
                            <ComboBoxItem Content="100" IsSelected="True"/>
                            <ComboBoxItem Content="500"/>
                            <ComboBoxItem Content="1000"/>
                           
                        </ComboBox>
                    </StackPanel>

                    <!-- Right Controls -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Border Style="{StaticResource RoundedGridStyle}" MinWidth="200" MaxWidth="300" Height="34">
                            <Grid>
                                <TextBox BorderThickness="0" Background="Transparent" FontFamily="Lexend" Name="txtSearch" Style="{StaticResource RoundedTextboxStyle}" VerticalAlignment="Center" Margin="10,0,40,0" />
                                <materialDesign:PackIcon Kind="Magnify" Width="25" Height="25" Foreground="Black" HorizontalAlignment="Right" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- DataGrid Container -->
            <Border Grid.Row="3" Background="White" CornerRadius="10" BorderBrush="White" BorderThickness="1" Padding="5">
                <DataGrid Name="dataGrid"
                      Style="{StaticResource CustomDataGridStyle}"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Margin="5"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Margin" Value="0,2,0,2"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="MinHeight" Value="85"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#EEF2FA"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E0E8F5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="FontFamily" Value="Lexend"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Background" Value="#474747"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="SeparatorBrush" Value="#5A5A5A"/>
                            <Setter Property="SeparatorVisibility" Value="Visible"/>
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                            <Setter Property="BorderBrush" Value="#5A5A5A"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.Columns>
                        <!-- ID Column -->
                        <DataGridTextColumn Header="#" Binding="{Binding ID}" Width="50" FontFamily="Lexend" Foreground="#474747"
                                            CellStyle="{StaticResource CenteredCellStyle}"/>

                        <!-- Product Image with rounded clipping -->
                        <DataGridTemplateColumn Header="Image" Width="100" MinWidth="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="8" Width="80" Height="80" Margin="8"
                                            Background="#F0F0F0" BorderBrush="#E0E0E0" BorderThickness="1"
                                            HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Border.Clip>
                                            <RectangleGeometry RadiusX="8" RadiusY="8" Rect="0,0,80,80"/>
                                        </Border.Clip>
                                        <Image Source="{Binding ImageSource}" Width="80" Height="80" Stretch="UniformToFill"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Text columns with responsive widths -->
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="1.5*" MinWidth="120" FontFamily="Lexend" Foreground="#333333"
                                            CellStyle="{StaticResource CenteredCellStyle}"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*" MinWidth="100" FontFamily="Lexend" Foreground="#555555"
                                            CellStyle="{StaticResource CenteredCellStyle}"/>
                        <DataGridTextColumn Header="Sub Category" Binding="{Binding SubCategory}" Width="*" MinWidth="110" FontFamily="Lexend" Foreground="#555555"
                                            CellStyle="{StaticResource CenteredCellStyle}"/>
                        <DataGridTextColumn Header="Brand" Binding="{Binding Brand}" Width="0.8*" MinWidth="90" FontFamily="Lexend" Foreground="#555555"
                                            CellStyle="{StaticResource CenteredCellStyle}"/>
                        <DataGridTextColumn Header="Supplier" Binding="{Binding Supplier}" Width="*" MinWidth="100" FontFamily="Lexend" Foreground="#555555"
                                            CellStyle="{StaticResource CenteredCellStyle}"/>
                        <DataGridTextColumn Header="Warehouse" Binding="{Binding Warehouse}" Width="*" MinWidth="100" FontFamily="Lexend" Foreground="#555555"
                                            CellStyle="{StaticResource CenteredCellStyle}"/>
                        <DataGridTextColumn x:Name="colSellingPrice" Header="Selling Price" Binding="{Binding SellingPrice}" Width="0.9*" MinWidth="100" FontFamily="Lexend" Foreground="#555555" CellStyle="{StaticResource CenteredCellStyle}"/>
                        <DataGridTextColumn x:Name="colBuyingPrice" Header="Buying Price" Binding="{Binding BuyingPrice}" Width="0.9*" MinWidth="100" FontFamily="Lexend" Foreground="#555555"
                                            CellStyle="{StaticResource CenteredCellStyle}"/>
                        <DataGridTextColumn Header="Stock Qty" Binding="{Binding StockQuantity}" Width="0.7*" MinWidth="85" FontFamily="Lexend" Foreground="Red"
                                            CellStyle="{StaticResource CenteredCellStyle}"/>
                        <DataGridTextColumn Header="Alert" Binding="{Binding AlertQuantity}" Width="0.6*" MinWidth="70" FontFamily="Lexend" Foreground="#555555"
                                            CellStyle="{StaticResource CenteredCellStyle}"/>

                        <!-- Action buttons - Fixed width to ensure visibility -->
                        <DataGridTemplateColumn Header="Actions" Width="100" MinWidth="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">

                                        <Button Width="32" Height="32" Background="Transparent" Margin="4,0" ToolTip="Edit" Click="OpenEditProduct" Cursor="Hand" BorderThickness="0">
                                            <materialDesign:PackIcon Kind="SquareEditOutline" Width="22" Height="22" Foreground="#52C69D"/>
                                        </Button>

                                        <Button Width="32" Height="32" Background="Transparent" Margin="4,0" ToolTip="Delete" Cursor="Hand" Click="DeleteProduct" BorderThickness="0">
                                            <materialDesign:PackIcon Kind="TrashCanOutline" Width="22" Height="22" Foreground="#E65E5E"/>
                                        </Button>

                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</UserControl>