<UserControl x:Class="DPC.Views.Stocks.ItemManager.NewProduct.ProductBatchEdit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="namespace:DPC.Views.Stocks.ItemManager" 
        xmlns:local1="namespace:DPC.Components.Forms"
        mc:Ignorable="d"
        Background="#D9D9D9"
        FontFamily="{StaticResource Lexend}"
        Foreground="#474747">

    <Grid>
        <!-- Main Form Content -->
        <Grid Margin="25,10,25,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Grid.ColumnSpan="3" Margin="0,0,0,0">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <materialDesign:PackIcon Kind="FormatListBulleted" Width="40" Height="40" Margin="0,0,15,0"/>
                    <TextBlock Text="Variation Details" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="#555555"/>
                </StackPanel>
            </StackPanel>

            <ScrollViewer Grid.Row="2"
                    VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Disabled"
                    VerticalAlignment="Stretch"
                    Style="{StaticResource ModernScrollViewerStyle}">
                <StackPanel Margin="0,0,0,0">
                    <StackPanel Margin="0,0,0,10" x:Name="StackPanelVariation">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Variations:" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,0"/>
                        </StackPanel>
                        <!--Warehouse Value-->
                        <Button Style="{StaticResource RoundedButtonStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="(White, Small)" FontSize="12" FontFamily="Lexend"  Foreground="#474747" FontWeight="SemiBold" Grid.Column="0" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                <materialDesign:PackIcon Kind="MenuRight" Foreground="#555555" Width="20" Height="20" Grid.Column="1" Margin="0,0,10,0"/>
                            </Grid>
                        </Button>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,10" x:Name="StackPanelWarehouse">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Warehouse:" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,0"/>
                        </StackPanel>
                        <!--Warehouse Value-->
                        <ComboBox x:Name="ComboBoxWarehouse" Style="{StaticResource RoundedComboBoxStyle}" Width="Auto" Height="40" SelectedIndex="0" Margin="0" FontFamily="Lexend"  Foreground="#474747" FontWeight="SemiBold"/>
                    </StackPanel>

                    <StackPanel Margin="0,10" x:Name="StackPanelRetailPrice">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <TextBlock Text="Selling Price:" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,0"/>
                            </StackPanel>
                            <Border Style="{StaticResource RoundedBorderStyle}">
                                <Grid>
                                    <!-- Define two columns: one for the icon, one for the TextBox -->
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Icon with Vertical Centering -->
                                    <materialDesign:PackIcon Kind="CurrencyPhp" Width="25" Height="20" Margin="10,0,0,0" VerticalAlignment="Center" Grid.Column="0"/>

                                    <!-- TextBox with Rounded Style -->
                                    <!-- Product Retail Price Value-->
                                    <TextBox x:Name="TxtRetailPrice" Style="{StaticResource RoundedTextboxStyle}" Margin="0,0,25,0" Grid.Column="1" 
                                    PreviewTextInput="IntegerOnlyTextInputHandler"
                                    DataObject.Pasting="IntegerOnlyPasteHandler" FontFamily="Lexend"  Foreground="#474747" FontWeight="SemiBold"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Margin="0,10,0,20" x:Name="StackPanelOrderPrice">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <TextBlock Text="Buying Price:" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,0"/>
                            </StackPanel>
                            <Border Style="{StaticResource RoundedBorderStyle}">
                                <Grid>
                                    <!-- Define two columns: one for the icon, one for the TextBox -->
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Icon with Vertical Centering -->
                                    <materialDesign:PackIcon Kind="CurrencyPhp" Width="25" Height="20" Margin="10,0,0,0" VerticalAlignment="Center" Grid.Column="0"/>

                                    <!-- TextBox with Rounded Style -->
                                    <!-- Product Purchase Order Value-->
                                    <TextBox x:Name="TxtPurchaseOrder" Style="{StaticResource RoundedTextboxStyle}" Margin="0,0,25,0" Grid.Column="1"
                                    PreviewTextInput="IntegerOnlyTextInputHandler"
                                    DataObject.Pasting="IntegerOnlyPasteHandler" FontFamily="Lexend"  Foreground="#474747" FontWeight="SemiBold"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,10" x:Name="StackPanelTaxRate">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Default Tax Rate:" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,0"/>
                        </StackPanel>
                        <Border Style="{StaticResource RoundedBorderStyle}">
                            <Grid>
                                <!-- Define two columns: one for the icon, one for the TextBox -->
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- TextBox with Rounded Style -->
                                <!-- Default Tax Rate-->
                                <TextBox x:Name="TxtDefaultTax" Style="{StaticResource RoundedTextboxStyle}" Margin="25,0,0,0" Grid.Column="0"
                                PreviewTextInput="IntegerOnlyTextInputHandler"
                                DataObject.Pasting="IntegerOnlyPasteHandler" FontFamily="Lexend"  Foreground="#474747" FontWeight="SemiBold"/>

                                <!-- Icon with Vertical Centering -->
                                <materialDesign:PackIcon Kind="PercentOutline" Width="25" Height="20" Margin="0,0,10,0" VerticalAlignment="Center" Grid.Column="1"/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Margin="0,10" x:Name="StackPanelDiscountRate">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Default Discount Rate:" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,0"/>
                        </StackPanel>
                        <Border Style="{StaticResource RoundedBorderStyle}">
                            <Grid>
                                <!-- Define two columns: one for the icon, one for the TextBox -->
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- TextBox with Rounded Style -->
                                <!-- Default Discount Rate-->
                                <TextBox x:Name="TxtDiscountRate" Style="{StaticResource RoundedTextboxStyle}" Margin="25,0,0,0" Grid.Column="0"
                                PreviewTextInput="IntegerOnlyTextInputHandler"
                                DataObject.Pasting="IntegerOnlyPasteHandler" FontFamily="Lexend"  Foreground="#474747" FontWeight="SemiBold"/>

                                <!-- Icon with Vertical Centering -->
                                <materialDesign:PackIcon Kind="PercentOutline" Width="25" Height="20" Margin="0,0,10,0" VerticalAlignment="Center" Grid.Column="1"/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Margin="0,10" x:Name="StackPanelStockUnits">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Stock Units:" FontSize="14" FontWeight="SemiBold" Margin="0,0,5,0"/>
                        </StackPanel>
                        <Border Style="{StaticResource RoundedBorderStyle}" x:Name="BorderStockUnits">
                            <!-- Stock Units Value-->
                            <TextBox x:Name="TxtStockUnits" Style="{StaticResource RoundedTextboxStyle}"
                            PreviewTextInput="IntegerOnlyTextInputHandler"
                            DataObject.Pasting="IntegerOnlyPasteHandler" FontFamily="Lexend"  Foreground="#474747" FontWeight="SemiBold"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Margin="0,10,0,0" x:Name="StackPanelAlertQuantity">
                        <StackPanel Orientation="Horizontal" Margin="0">
                            <TextBlock Text="Alert Quantity:" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,0"/>
                        </StackPanel>
                        <Border Style="{StaticResource RoundedBorderStyle}">
                            <!-- Alert Quantity Value-->
                            <TextBox x:Name="TxtAlertQuantity" Style="{StaticResource RoundedTextboxStyle}"      
                            PreviewTextInput="IntegerOnlyTextInputHandler"
                            DataObject.Pasting="IntegerOnlyPasteHandler" FontFamily="Lexend"  Foreground="#474747" FontWeight="SemiBold"/>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="3" Margin="0,10,0,0">
                <!-- Add Product Button -->
                <Button x:Name="BtnSaveBatchEdit" Style="{StaticResource RoundedButtonStyle}" Background="#52C69D" BorderThickness="0" Click="BtnSaveBatchEdit_Click" Margin="0,10,0,0">
                    <TextBlock Text="Save Batch Edit"
                            Foreground="White"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,0"/>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>