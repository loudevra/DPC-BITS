<UserControl x:Class="DPC.Views.Stocks.Supplier.NewSuppliers.NewSuppliers"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Background="Transparent"
        FontFamily="{StaticResource Lexend}"
        Foreground="#474747"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        Unloaded="NewSupplierUserControl_Unloaded"
        Loaded="NewSupplierUserControl_Loaded">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">

        <Border Background="#FFFFFF" 
                HorizontalAlignment="Stretch" 
                Margin="10"
                CornerRadius="15">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="30,30,30,10">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Target" Foreground="#474747" Width="30" Height="30" VerticalAlignment="Center"/>
                        <TextBlock Text="Add New Supplier" FontWeight="Bold" FontSize="26" FontFamily="{StaticResource Lexend}" Foreground="#474747" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>

                    <Border Height="1" Background="#E0E0E0" Margin="0,15,0,0" HorizontalAlignment="Stretch"/>
                </StackPanel>

                <ScrollViewer Style="{StaticResource ModernScrollViewerStyle}" 
                              VerticalScrollBarVisibility="Auto" 
                              HorizontalScrollBarVisibility="Disabled"
                              Grid.Row="1" Margin="30,0,15,30" Padding="0,0,15,0">

                    <StackPanel HorizontalAlignment="Stretch">

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Company Representative:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                                <TextBox x:Name="TxtRepresentative" Style="{StaticResource RoundedTextboxStyle}" FontFamily="Lexend" FontWeight="SemiBold" CharacterCasing="Upper" TextChanged="ForceUppercase_TextChanged"/>
                            </Border>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Company Name:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                                <TextBox x:Name="TxtCompany" Style="{StaticResource RoundedTextboxStyle}" FontFamily="Lexend" FontWeight="SemiBold" CharacterCasing="Upper" TextChanged="ForceUppercase_TextChanged"/>
                            </Border>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Phone Number:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                                <TextBox x:Name="TxtPhone" PreviewTextInput="TxtPhone_PreviewTextInput" Style="{StaticResource RoundedTextboxStyle}" FontFamily="Lexend" FontWeight="SemiBold"/>
                            </Border>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Email:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                                <TextBox x:Name="TxtEmail" Style="{StaticResource RoundedTextboxStyle}" FontFamily="Lexend" FontWeight="SemiBold"/>
                            </Border>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Brand/s:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <Grid>
                                <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                                    <TextBox x:Name="TxtItem" Style="{StaticResource RoundedTextboxStyle}" VerticalAlignment="Center" FontFamily="Lexend" FontWeight="SemiBold" CharacterCasing="Upper" TextChanged="ForceUppercase_TextChanged" />
                                </Border>
                                <Popup x:Name="AutoCompletePopup" PlacementTarget="{Binding ElementName=TxtItem}" Placement="Bottom" StaysOpen="False" IsOpen="False" AllowsTransparency="True" PopupAnimation="Fade">
                                    <Border Background="White" BorderBrush="LightGray" BorderThickness="1" MaxHeight="150" Width="{Binding ElementName=TxtItem, Path=ActualWidth}">
                                        <ListBox x:Name="LstItems">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Name}" Padding="8"/>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </Border>
                                </Popup>
                            </Grid>
                            <WrapPanel x:Name="ChipPanel" Margin="0,5,0,0" />
                        </StackPanel>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Address:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                                <TextBox x:Name="TxtAddress" Style="{StaticResource RoundedTextboxStyle}" FontFamily="Lexend" FontWeight="SemiBold" CharacterCasing="Upper" TextChanged="ForceUppercase_TextChanged"/>
                            </Border>
                        </StackPanel>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="City:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                                    <TextBox x:Name="TxtCity" Style="{StaticResource RoundedTextboxStyle}" FontFamily="Lexend" FontWeight="SemiBold" CharacterCasing="Upper" TextChanged="ForceUppercase_TextChanged"/>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Region:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                                    <TextBox x:Name="TxtRegion" Style="{StaticResource RoundedTextboxStyle}" FontFamily="Lexend" FontWeight="SemiBold" CharacterCasing="Upper" TextChanged="ForceUppercase_TextChanged"/>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Country:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                                    <TextBox x:Name="TxtCountry" PreviewTextInput="TxtCountry_PreviewTextInput" Style="{StaticResource RoundedTextboxStyle}" FontFamily="Lexend" FontWeight="SemiBold" CharacterCasing="Upper" TextChanged="ForceUppercase_TextChanged"/>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Postal Code:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                                <TextBox x:Name="TxtPostalCode" PreviewTextInput="TxtPostalCode_PreviewTextInput" Style="{StaticResource RoundedTextboxStyle}" FontFamily="Lexend" FontWeight="SemiBold"/>
                            </Border>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,25">
                            <TextBlock Text="TIN ID:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                                <TextBox x:Name="TxtTINID" PreviewTextInput="TxtTINID_PreviewTextInput" Style="{StaticResource RoundedTextboxStyle}" FontFamily="Lexend" FontWeight="SemiBold"/>
                            </Border>
                        </StackPanel>

                        <Button Width="130" Height="40" HorizontalAlignment="Right" Style="{StaticResource RoundedButtonStyle}" BorderThickness="0" Background="#52C69D" Click="BtnAddSupplier">
                            <TextBlock Text="Add" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" FontSize="15" FontWeight="Bold"/>
                        </Button>

                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>