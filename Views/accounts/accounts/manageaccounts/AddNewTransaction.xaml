<UserControl x:Class="DPC.Views.Accounts.Accounts.ManageAccounts.AddNewTransaction"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="Transparent">

    <UserControl.Resources>

        <Style x:Key="RoundedComboBoxStyle" TargetType="ComboBox">
            <Setter Property="FontFamily" Value="{StaticResource Lexend}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#474747"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#474747"/>

            <!-- Control Template with No Hover Effect and Scrollable Dropdown -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border x:Name="ComboBorder"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            CornerRadius="20"
            Padding="0">
                            <Grid>
                                <!-- ComboBox Content -->
                                <ContentPresenter Margin="10,0,30,0"
                              VerticalAlignment="Center"
                              Content="{TemplateBinding SelectionBoxItem}"
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>

                                <!-- Dropdown Toggle Button -->
                                <ToggleButton x:Name="ToggleButton"
                          IsChecked="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                          Focusable="False"
                          Background="Transparent"
                          BorderThickness="0"
                          Style="{StaticResource ComboBoxToggleButton}"/>

                                <!-- Default Popup for Dropdown -->
                                <Popup x:Name="Popup"
                   Placement="Bottom"
                   IsOpen="{TemplateBinding IsDropDownOpen}"
                   AllowsTransparency="True"
                   PopupAnimation="Slide"
                   Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <Border Background="White"
                        CornerRadius="10"
                        BorderBrush="Gray"
                        BorderThickness="1"
                        MaxHeight="200">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedTextboxStyle" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"/>
                            <TextBlock Text="{TemplateBinding Tag}"
                               FontFamily="Lexend"
                               FontSize="14"
                               Foreground="#888"
                               Margin="15,0"
                               VerticalAlignment="Center"
                               IsHitTestVisible="False"
                               Visibility="Collapsed"
                               x:Name="Placeholder"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded Border Style -->
        <Style x:Key="RoundedBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="25"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>

        <!-- Rounded Button Style -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2E6651"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="25">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="10" HorizontalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Title -->
                <TextBlock Grid.Row="0" Text="Add New Transaction"
                           FontWeight="Bold" FontFamily="Lexend" FontSize="35" Foreground="#474747"
                           Margin="0,10,0,20" HorizontalAlignment="Left"/>

                <!-- Toggle + Search -->
                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <Border Background="#555" CornerRadius="25,0,0,25" Padding="20,10" MinWidth="130">
                            <TextBlock Text="Customer" Foreground="White" FontFamily="Lexend" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <Border BorderBrush="#555" BorderThickness="2" Background="White" CornerRadius="0,25,25,0" Padding="20,10" MinWidth="130">
                            <TextBlock Text="Supplier" Foreground="#555" FontFamily="Lexend" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </StackPanel>

                    <Border Grid.Column="2" Style="{StaticResource RoundedBorderStyle}" Height="40">
                        <TextBox Style="{StaticResource RoundedTextboxStyle}" FontFamily="Lexend" Foreground="#474747"
                                 Tag="Enter person name or mobile number (optional)"/>
                    </Border>
                </Grid>

                <!-- Divider -->
                <Border Grid.Row="2" Height="1" Background="#AEAEAE" Margin="0,0,0,20"/>

                <!-- Row 3 -->
                <Grid Grid.Row="3" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="C/O:" FontFamily="Lexend" FontWeight="Bold" FontSize="14" Foreground="#474747"/>
                        <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                            <TextBox Style="{StaticResource RoundedTextboxStyle}" Tag="Enter product code..." FontFamily="Lexend" Foreground="#474747"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                        <TextBlock Text="To Account:" FontFamily="Lexend" FontWeight="Bold" FontSize="14" Foreground="#474747"/>
                        <ComboBox Style="{StaticResource RoundedComboBoxStyle}" Height="40" Tag="01 - DREAM PC" SelectedIndex="-1" FontFamily="Lexend" Foreground="#474747">
                            <ComboBoxItem Content="Select Account..."/>
                            <ComboBoxItem Content="01 - DREAM PC"/>
                        </ComboBox>

                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Amount:" FontFamily="Lexend" FontWeight="Bold" FontSize="14" Foreground="#474747"/>
                        <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                            <TextBox Style="{StaticResource RoundedTextboxStyle}" Tag="Enter amount..." FontFamily="Lexend" Foreground="#474747"/>
                        </Border>
                    </StackPanel>
                </Grid>

                <!-- Divider -->
                <Border Grid.Row="4" Height="1" Background="#AEAEAE" Margin="0,0,0,20"/>

                <!-- Row 4 -->
                <Grid Grid.Row="5" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,0" Grid.Column="1">
                            <TextBlock Text="Date:" FontFamily="Lexend" FontWeight="Bold" FontSize="14" Foreground="#474747"/>
                        </StackPanel>
                        <Border Style="{StaticResource RoundedBorderStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width=".05*"/>
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <!-- Single Date Picker -->
                                <DatePicker x:Name="SingleDatePicker" FontFamily="Lexend" Foreground="#474747"
Style="{StaticResource SingleDatePickerStyle}"
                                    SelectedDate="{Binding SelectedDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
          
                                    SelectedDateChanged="SingleDatePicker_SelectedDateChanged"
                                    Opacity="0"
                                    IsHitTestVisible="False" />
                                <!-- Calendar Icon -->
                                <materialDesign:PackIcon Grid.Column="0" Kind="Calendar" Width="32" Height="25" Foreground="#474747" VerticalAlignment="Center" Grid.ColumnSpan="2" Margin="10,0,0,0"/>
                                <!-- Date Display - Now uses FormattedDate property -->
                                <Button x:Name="DateButton" Grid.Column="1" Style="{StaticResource DateButtonStyle}" Click="StartDate_Click" Width="Auto" Margin="28,0,25,0">
                                    <TextBlock Text="{Binding FormattedDate}" Margin="0,0,5,0" Foreground="#474747" Height="14"/>
                                </Button>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                        <TextBlock Text="Type:" FontFamily="Lexend" FontWeight="Bold" FontSize="14" Foreground="#474747"/>
                        <ComboBox Style="{StaticResource RoundedComboBoxStyle}" FontFamily="Lexend" Foreground="#474747">
                            <ComboBoxItem Content="Income / Credit"/>
                            <ComboBoxItem Content="Expense / Debit"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Category:" FontFamily="Lexend" FontWeight="Bold" FontSize="14" Foreground="#474747"/>
                        <ComboBox Style="{StaticResource RoundedComboBoxStyle}" IsSelected="True" Height="40" FontFamily="Lexend" Foreground="#474747">
                            <ComboBoxItem Content="Income"/>
                            <ComboBoxItem Content="Expenses"/>
                            <ComboBoxItem Content="Other"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <!-- Divider -->
                <Border Grid.Row="6" Height="1" Background="#AEAEAE" Margin="0,0,0,20"/>

                <!-- Row 5 -->
                <Grid Grid.Row="7" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Method:" FontFamily="Lexend" FontWeight="Bold" FontSize="14" Foreground="#474747"/>
                        <ComboBox Style="{StaticResource RoundedComboBoxStyle}" IsSelected="True" Height="40" FontFamily="Lexend" Foreground="#474747">
                            <ComboBoxItem Content="Cash"/>
                            <ComboBoxItem Content="Card"/>
                            <ComboBoxItem Content="Cheque"/>
                            <ComboBoxItem Content="Bank"/>
                            <ComboBoxItem Content="Other"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Note:" FontFamily="Lexend" FontWeight="Bold" FontSize="14" Foreground="#474747"/>
                        <Border Style="{StaticResource RoundedBorderStyle}" Height="40">
                            <TextBox Style="{StaticResource RoundedTextboxStyle}" Tag="Optional note..." FontFamily="Lexend" Foreground="#474747"/>
                        </Border>
                    </StackPanel>
                </Grid>

                <!-- Divider -->
                <Border Grid.Row="8" Height="1" Background="#AEAEAE" Margin="0,0,0,20"/>

                <!-- Button -->
                <Button Grid.Row="9" Content="Add Transaction"
                    Style="{StaticResource RoundedButtonStyle}"
                    HorizontalAlignment="Stretch" Margin="0,10,0,0"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>