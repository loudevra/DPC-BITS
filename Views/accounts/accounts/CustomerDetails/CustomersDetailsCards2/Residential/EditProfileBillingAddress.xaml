<UserControl x:Class="DPC.Views.Accounts.Accounts.CustomerDetails.CustomersDetailsCards2.Residential.EditProfileBillingAddress"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Rounded TextBox Style -->
        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="20"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded ComboBox ToggleButton Style -->
        <Style x:Key="ComboBoxToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="Transparent" BorderBrush="Transparent" BorderThickness="0">
                            <materialDesign:PackIcon Kind="ChevronDown"
                                                     Width="18"
                                                     Height="18"
                                                     Foreground="Gray"
                                                     HorizontalAlignment="Center"
                                                     VerticalAlignment="Center"
                                                     Margin="0,0,8,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded ComboBox Style -->
        <Style x:Key="RoundedComboBoxStyle" TargetType="ComboBox">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="20"
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <ContentPresenter x:Name="ContentSite"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      Margin="3,0,30,0"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"/>
                                    <ToggleButton x:Name="ToggleButton"
                                                  Style="{StaticResource ComboBoxToggleButtonStyle}"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  Focusable="False"
                                                  ClickMode="Press"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid MinWidth="{TemplateBinding ActualWidth}" MaxHeight="200">
                                    <Border x:Name="DropDownBorder"
                                            Background="White"
                                            BorderBrush="#555555"
                                            BorderThickness="1"
                                            CornerRadius="10"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True"
                                                    KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="35"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="#EEE"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#AAA"/>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="True">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded Button Style -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#86BCD5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#73404040" 
            CornerRadius="20" 
            Padding="0"
            BorderThickness="0">

        <Grid>
            <!-- Header -->
            <Border Background="#555555" Height="56" VerticalAlignment="Top" CornerRadius="20,20,0,0" Margin="0,0,-94,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Kind="AccountEdit" Width="24" Height="24" Margin="20,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Column="0" Foreground="#FDFDFD"/>
                    <TextBlock Text="Edit Profile" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="8,0,0,0" FontWeight="Normal" FontSize="20" Foreground="#FDFDFD" Grid.Column="1"/>
                    <Button Grid.Column="2" Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets" 
                            Background="Transparent" BorderThickness="0" Foreground="White"
                            Width="30" Height="30" Margin="0,0,20,0"
                            VerticalAlignment="Center" HorizontalAlignment="Right" Cursor="Hand"/>
                </Grid>
            </Border>

            <Border Background="White" CornerRadius="0,0,20,20" Margin="0,56,-94,0">
                <Grid Margin="0,20,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Tabs -->
                    <StackPanel Orientation="Horizontal" Margin="30,0,0,0" VerticalAlignment="Center" Height="40">
                        <Button Content="Personal Info" 
                                FontWeight="SemiBold"
                                BorderThickness="0" 
                                Background="Transparent"
                                Foreground="#AEAEAE"
                                Padding="0"
                                Width="100"
                                Cursor="Hand"
                                FontSize="14"
                                FontFamily="lexend"
                                VerticalAlignment="Center"
                                Height="40"/>
                        <Button Content="Billing Address" 
                                FontWeight="SemiBold"
                                BorderThickness="0,0,0,2" 
                                BorderBrush="#AEAEAE"
                                Background="Transparent"
                                Foreground="#555555"
                                Padding="0"
                                Width="120"
                                Cursor="Hand"
                                FontSize="14"
                                FontFamily="lexend"
                                VerticalAlignment="Center"
                                Height="40"/>
                        <Button Content="Shipping Address" Background="Transparent" BorderThickness="0" Foreground="#AEAEAE" Width="130" Cursor="Hand" FontFamily="lexend" FontSize="14" VerticalAlignment="Center" Height="40" FontWeight="SemiBold"/>
                        <Button Content="Other" Background="Transparent" BorderThickness="0" Foreground="#AEAEAE" Width="90" Cursor="Hand" FontFamily="lexend" FontSize="14" VerticalAlignment="Center" Height="40" FontWeight="SemiBold"/>
                    </StackPanel>

                    <!-- Fields -->
                    <StackPanel Grid.Row="1" Margin="30,30,30,0">
                        <!-- Address -->
                        <TextBlock Text="Address:" Margin="0,0,0,4" FontSize="14" FontFamily="lexend" FontWeight="SemiBold" Foreground="#474747"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <!-- Avatar Placeholder -->
                            <!-- Address Box -->
                            <TextBox
                                     Height="40" FontSize="14" BorderBrush="#474747" 
                                     Padding="15,0" 
                                     Background="White"
                                     Style="{StaticResource RoundedTextBoxStyle}" 
                                     Foreground="#47474780" Grid.ColumnSpan="2"/>
                        </Grid>

                        <!-- City, Region, Country -->
                        <Grid Margin="0,20,0,0" HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="City:" Margin="0,0,0,4" FontSize="14" FontFamily="lexend" FontWeight="SemiBold" Foreground="#474747"/>
                                <ComboBox BorderBrush="#474747" Foreground="#47474780"
                                          Style="{StaticResource RoundedComboBoxStyle}" />
                            </StackPanel>
                            <!-- Spacer in column 1 -->
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Region:" Margin="0,0,0,4" FontSize="14" FontFamily="lexend" FontWeight="SemiBold" Foreground="#474747"/>
                                <ComboBox BorderBrush="#474747" Foreground="#47474780"
                                          Style="{StaticResource RoundedComboBoxStyle}" />
                            </StackPanel>
                            <!-- Spacer in column 3 -->
                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Country:" Margin="0,0,0,4" FontSize="14" FontFamily="lexend" FontWeight="SemiBold" Foreground="#474747"/>
                                <ComboBox BorderBrush="#474747" Foreground="#474747"
                                          Style="{StaticResource RoundedComboBoxStyle}" />
                            </StackPanel>
                        </Grid>

                        <!-- ZIP Code -->
                        <TextBlock Text="ZIP Code:" Margin="0,20,0,4" FontSize="14" FontFamily="lexend" FontWeight="SemiBold" Foreground="#474747"/>
                        <TextBox Height="40" FontSize="14" BorderBrush="#474747" BorderThickness="1" 
                                 Padding="15,0" Margin="0,0,0,32" 
                                 Background="White"
                                 HorizontalAlignment="Stretch"
                                 Style="{StaticResource RoundedTextBoxStyle}" Foreground="#47474780"/>

                        <!-- Save Button -->
                        <Button Content="Save"
                                Width="140"
                                Height="40"
                                HorizontalAlignment="Right"
                                Background="#86BCD5"
                                Foreground="White"
                                FontWeight="SemiBold"
                                BorderThickness="0"
                                Margin="0,0,0,0"
                                Cursor="Hand"
                                FontSize="14"
                                Style="{StaticResource RoundedButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>