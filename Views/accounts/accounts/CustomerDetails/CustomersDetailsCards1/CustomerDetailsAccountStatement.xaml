<UserControl x:Class="DPC.Views.Accounts.Accounts.CustomerDetails.CustomerDetailsCards1.CustomerDetailsAccountStatement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            
             mc:Ignorable="d"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             Background="Transparent"
             MinWidth="0"
             MinHeight="0">

    <UserControl.Resources>
        <!-- Rounded Border -->
        <Style x:Key="RoundedBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="15"/>
        </Style>

        <!-- DatePicker Style -->
        <Style x:Key="RoundedDatePickerStyle" TargetType="DatePicker">
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border Style="{StaticResource RoundedBorderStyle}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <DatePickerTextBox x:Name="PART_TextBox"
                                               Background="Transparent"
                                               BorderThickness="0"
                                               VerticalContentAlignment="Center"
                                               Padding="10,0"
                                               Foreground="Black"/>
                                <Button x:Name="PART_Button"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Width="30"
                                    Height="20"
                                    Padding="0"
                                    Margin="0"
                                    BorderThickness="0"
                                    Background="Transparent">
                                    <materialDesign:PackIcon Kind="CalendarOutline" Width="20" Height="20" Foreground="#555555"/>
                                </Button>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded ComboBox -->
        <Style x:Key="RoundedComboBoxStyle" TargetType="ComboBox">
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Main Border -->
                            <Border x:Name="MainBorder"
                        Style="{StaticResource RoundedBorderStyle}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="35" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Content -->
                                    <ContentPresenter Name="ContentSite"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          VerticalAlignment="Center"
                                          Margin="10,0,5,0"
                                          RecognizesAccessKey="True" />

                                    <!-- Drop Down Button -->
                                    <ToggleButton Grid.Column="1"
                                      Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                      Background="Transparent"
                                      BorderBrush="Transparent"
                                      BorderThickness="0"
                                      HorizontalContentAlignment="Center"
                                      VerticalContentAlignment="Center">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                                                    <materialDesign:PackIcon Kind="ChevronDown"
                                                                 Width="20"
                                                                 Height="20"
                                                                 Foreground="#555555"
                                                                 VerticalAlignment="Center"
                                                                 HorizontalAlignment="Center"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                            </Border>

                            <!-- Dropdown Popup -->
                            <Popup Name="Popup"
                       Placement="Bottom"
                       IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                       AllowsTransparency="True"
                       Focusable="False"
                       StaysOpen="False"
                       MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                <Border Background="White"
                            BorderBrush="#CCCCCC"
                            BorderThickness="1"
                            CornerRadius="15"
                            SnapsToDevicePixels="True"
                            Margin="0,5,0,0">
                                    <ScrollViewer Margin="0"
                                      SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ViewButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#86BCD5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border 
                Background="{TemplateBinding Background}" 
                CornerRadius="20" 
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}">
                            <TextBlock Text="View"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontFamily="Lexend"
                           FontSize="16"
                           Foreground="White"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <!-- Main Layout -->
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Account Statements"
                       FontWeight="SemiBold"
                       FontFamily="Lexend"
                       FontSize="25"
                       Foreground="#474747"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Type -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Type:" Margin="0,0,0,5" FontFamily="Lexend" Foreground="#555555" FontWeight="SemiBold"/>
            <ComboBox Style="{StaticResource RoundedComboBoxStyle}" >
                <ComboBoxItem Content="All Transactions" IsSelected="True"/>
                <ComboBoxItem Content="Deposits"/>
                <ComboBoxItem Content="Withdrawals"/>
                <ComboBoxItem Content="Transfers"/>
            </ComboBox>
        </StackPanel>

        <!-- From Date -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="From Date:" Margin="0,0,0,5" FontFamily="Lexend" Foreground="#555555" FontWeight="SemiBold"/>
            <DatePicker Style="{StaticResource RoundedDatePickerStyle}" SelectedDate="2025-03-12" Margin="0,0,0,5"/>
        </StackPanel>

        <!-- To Date -->
        <StackPanel Grid.Row="3" Margin="0,0,0,20">
            <TextBlock Text="To Date:" Margin="0,0,0,5" FontFamily="Lexend" Foreground="#555555" FontWeight="SemiBold"/>
            <DatePicker Style="{StaticResource RoundedDatePickerStyle}" SelectedDate="2025-03-12" Margin="0,0,0,5"/>
        </StackPanel>
        <StackPanel Grid.Row="4">
            <Button Style="{StaticResource ViewButtonStyle}" />
        </StackPanel>
    </Grid>
</UserControl>




