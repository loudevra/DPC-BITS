<UserControl x:Class="DPC.Views.Sales.Quotes.EditQuote"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Background="#D9D9D9" FontFamily="Lexend"
             d:DesignHeight="900" d:DesignWidth="1100">

    <UserControl.Resources>
        <Style x:Key="RoundedBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#AEAEAE"/>
        </Style>
        <Style x:Key="RoundedTextboxStyle" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="ModernDataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1D3242"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Lexend"/>
            <Setter Property="Padding" Value="5,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#2D4252"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="25,20,25,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <materialDesign:PackIcon Kind="Calculator" Width="30" Height="30" Foreground="#474747" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Edit Cost Estimate" FontWeight="SemiBold" FontSize="25" Foreground="#474747" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,15,0">
                    <TextBlock Text="Search Client:" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#474747"/>
                    <Border Style="{StaticResource RoundedBorderStyle}" Background="White" Height="40">
                        <Grid>
                            <TextBox x:Name="txtSearchCustomer" TextChanged="txtSearchCustomer_TextChanged" Style="{StaticResource RoundedTextboxStyle}" Padding="10,0"/>
                            <materialDesign:PackIcon Kind="Magnify" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#AEAEAE"/>
                        </Grid>
                    </Border>

                    <Popup x:Name="AutoCompletePopup" PlacementTarget="{Binding ElementName=txtSearchCustomer}" Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
                        <Border Background="White" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5">
                            <ListBox x:Name="LstItems" SelectionChanged="LstItems_SelectionChanged" MaxHeight="150" Width="{Binding ElementName=txtSearchCustomer, Path=ActualWidth}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" Padding="8"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Border>
                    </Popup>

                    <TextBlock Text="Client Details:" FontWeight="SemiBold" Margin="0,15,0,5" Foreground="#474747"/>
                    <Border Style="{StaticResource RoundedBorderStyle}" Background="White">
                        <TextBox x:Name="TxtClientDetails" Height="150" TextWrapping="Wrap" AcceptsReturn="True" Style="{StaticResource RoundedTextboxStyle}" Padding="10" IsReadOnly="True" VerticalContentAlignment="Top"/>
                    </Border>

                    <TextBlock Text="Warehouse Source:" FontWeight="SemiBold" Margin="0,15,0,5" Foreground="#474747"/>
                    <ComboBox x:Name="ComboBoxWarehouse" SelectionChanged="ComboBoxWarehouse_SelectionChanged" Style="{StaticResource RoundedComboBoxStyle}" Height="40" Background="White"/>
                </StackPanel>

                <Border Grid.Column="1" Background="White" CornerRadius="15" Padding="20" BorderBrush="#AEAEAE" BorderThickness="1" Margin="15,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Quote Properties" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#1D3242"/>

                        <TextBlock Text="Cost Estimate Type:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbCostEstimateType" SelectionChanged="cmbCostEstimateType_SelectionChanged" Style="{StaticResource RoundedComboBoxStyle}" Height="35" Background="White" Margin="0,0,0,10">
                            <ComboBoxItem Content="Walk-In CE (Walk In)"/>
                            <ComboBoxItem Content="Household CE (Residential)"/>
                            <ComboBoxItem Content="Government Project CE (Government)"/>
                            <ComboBoxItem Content="Business CE (Corporate)"/>
                        </ComboBox>

                        <TextBlock Text="Quote Number:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <Border Style="{StaticResource RoundedBorderStyle}" Height="35" Margin="0,0,0,10" Background="#F5F5F5">
                            <TextBox x:Name="txtQuoteNumber" TextChanged="txtQuoteNumber_TextChanged" IsReadOnly="False" Style="{StaticResource RoundedTextboxStyle}" Padding="10,0" FontWeight="Bold"/>
                        </Border>

                        <TextBlock Text="Quote Date:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <Border Style="{StaticResource RoundedBorderStyle}" Height="35" Margin="0,0,0,10">
                            <Grid>
                                <DatePicker x:Name="QuoteDate" Visibility="Hidden"/>
                                <Button x:Name="QuoteDateButton" Click="QuoteDateButton_Click" Style="{StaticResource DateButtonStyle}" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch" Padding="10,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding FormattedDate}" VerticalAlignment="Center" Grid.Column="0"/>
                                        <materialDesign:PackIcon Kind="CalendarMonth" Grid.Column="1" Foreground="#474747"/>
                                    </Grid>
                                </Button>
                            </Grid>
                        </Border>

                        <TextBlock Text="Validity Period:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbCostEstimateValidty" SelectionChanged="cmbCostEstimateValidty_SelectionChanged" Style="{StaticResource RoundedComboBoxStyle}" Height="35" Background="White" Margin="0,0,0,10">
                            <ComboBoxItem Content="48 Hours" IsSelected="True"/>
                            <ComboBoxItem Content="1 Week"/>
                            <ComboBoxItem Content="2 Weeks"/>
                            <ComboBoxItem Content="1 Month"/>
                        </ComboBox>

                        <TextBlock Text="Note:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <Border Style="{StaticResource RoundedBorderStyle}" MinHeight="80">
                            <TextBox x:Name="txtQuoteNote" TextWrapping="Wrap" AcceptsReturn="True" Style="{StaticResource RoundedTextboxStyle}" Padding="10" VerticalContentAlignment="Top"/>
                        </Border>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,25,0,0">
                    <DataGrid x:Name="ProductInfoDataGrid" Background="Transparent" BorderThickness="0" AutoGenerateColumns="False" 
                              CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="None"
                              ColumnHeaderStyle="{StaticResource ModernDataGridHeaderStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ITEM DESCRIPTION" Width="160"/>
                            <DataGridTextColumn Header="QTY" Width="60"/>
                            <DataGridTextColumn Header="UNIT PRICE" Width="95"/>
                            <DataGridTextColumn x:Name="TaxHeader" Header="TAX (%)" Width="75"/>
                            <DataGridTextColumn Header="TAX VALUE" Width="75"/>
                            <DataGridTextColumn Header="DISC (%)" Width="85"/>
                            <DataGridTextColumn Header="DISCOUNT" Width="85"/>
                            <DataGridTextColumn Header="TOTAL" Width="100"/>
                            <DataGridTextColumn Header="" Width="50"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <ScrollViewer MaxHeight="1070" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="MainContainer" Background="Transparent"/>
                    </ScrollViewer>

                    <Button Click="AddNewRow_Click" HorizontalAlignment="Center" Margin="0,15,0,0" Style="{StaticResource MaterialDesignOutlinedButton}" BorderBrush="#1D3242" Foreground="#1D3242" Height="35">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="PlaylistAdd" Margin="0,0,5,0"/>
                            <TextBlock Text="Add New Item Row"/>
                        </StackPanel>
                    </Button>
                </StackPanel>


                <Grid Grid.Row="2" Grid.ColumnSpan="2" Margin="0,30,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Rectangle Grid.Row="0" Grid.ColumnSpan="2" Height="1" Fill="#AEAEAE" Margin="0,0,0,25"  HorizontalAlignment="Stretch"/>

                    <StackPanel Grid.Column="0" Grid.Row="1">
                        <TextBlock Text="Calculation Settings" FontWeight="Bold" Foreground="#1D3242" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Tax Mode:" Width="120" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <ComboBox x:Name="txtTaxSelection" SelectionChanged="txtTaxSelection_SelectionChanged" Width="180" Style="{StaticResource RoundedComboBoxStyle}">
                                <ComboBoxItem Content="Inclusive" IsSelected="True"/>
                                <ComboBoxItem Content="Exclusive"/>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Discount Mode:" Width="120" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <ComboBox x:Name="txtDiscountSelection" Width="220" Style="{StaticResource RoundedComboBoxStyle}">
                                <ComboBoxItem Content="% Discount After" IsSelected="True"/>
                                <ComboBoxItem Content="Flat Discount After"/>
                                <ComboBoxItem Content="% Discount Before"/>
                                <ComboBoxItem Content="Flat Discount Before"/>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Grid.Row="1">
                        <Border Background="White" CornerRadius="15" Padding="20" BorderBrush="#AEAEAE" BorderThickness="1">
                            <StackPanel>
                                <Grid Margin="0,0,0,8">
                                    <TextBlock Text="Total Tax" Foreground="#555555"/>
                                    <TextBlock x:Name="txtTotalTax" Text="₱ 0.00" HorizontalAlignment="Right" FontWeight="Bold"/>
                                </Grid>
                                <Grid Margin="0,0,0,8">
                                    <TextBlock Text="Total Discount" Foreground="#555555"/>
                                    <TextBlock x:Name="txtTotalDiscount" Text="₱ 0.00" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#D23636"/>
                                </Grid>
                                <Separator Margin="0,10" Background="#AEAEAE"/>
                                <Grid>
                                    <TextBlock Text="GRAND TOTAL" FontSize="18" FontWeight="Bold" Foreground="#1D3242"/>
                                    <TextBlock x:Name="txtGrandTotal" Text="₱ 0.00" FontSize="24" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#1D5642"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!--<StackPanel x:Name="ShowVatExBtn" Visibility="Collapsed" Margin="0,10,0,0" >
                            <Button x:Name="IncExVatinExclusive" Click="IncExVatinExclusive_Click" Background="#1D5642" Style="{StaticResource MaterialDesignRaisedButton}">
                                <TextBlock x:Name="VatExShowVat" Text="Show VAT 12%"/>
                            </Button>
                        </StackPanel>-->

                        <Button x:Name="GenerateCostEstimate" Click="UpdateCostEstimate_Click" Margin="0,15,0,0" Height="55" Background="#1D5642" BorderBrush="#1D5642" Style="{StaticResource MaterialDesignRaisedButton}" materialDesign:ButtonAssist.CornerRadius="27">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileSend" Width="24" Height="24" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="White"/>
                                <TextBlock Text="UPDATE QUOTATION" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Foreground="White"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>