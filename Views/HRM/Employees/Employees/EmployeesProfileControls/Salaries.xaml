<UserControl x:Class="DPC.Views.HRM.Employees.Employees.EmployeesProfile.EmployeesProfileControls.Salaries"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:DPC"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <x:Array x:Key="EmptyItems" Type="{x:Type sys:Object}">
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
            <sys:Object />
        </x:Array>
        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="25"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0"  LastChildFill="False">
            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                <TextBlock FontFamily="{StaticResource Lexend}" VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Foreground="#474747" Margin="0,0,20,0">Salaries</TextBlock>
                <Border Style="{StaticResource RoundedBorderStyle}" Padding="7,1"  CornerRadius="10" BorderBrush="#9D9D9D" BorderThickness="1.1" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Foreground="#9D9D9D" Margin="0,0,5,0" Height="17" Width="17" Kind="CashMultiple" VerticalAlignment="Center"/>
                        <TextBlock Foreground="#9D9D9D" FontSize="10" FontWeight="SemiBold" VerticalAlignment="Center" Text="Update Salary"/>
                    </StackPanel>
                </Border>
            </StackPanel>
            <materialDesign:PackIcon Kind="FolderMoveOutline" Height="24" Width="24" Foreground="#474747" DockPanel.Dock="Right"/>
        </DockPanel>
        <DataGrid 
        Margin="0, 10,0,0"
        Grid.Row="1"
        ItemsSource="{StaticResource EmptyItems}"
        Style="{StaticResource DataGridStyle}"
        VerticalScrollBarVisibility="Visible"
        >

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10,0"/>
                    <Setter Property="BorderBrush" Value="#d9d9d9"/>
                    <Setter Property="BorderThickness" Value="0, 0, 0, 1"/>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.RowHeaderStyle>
                <Style TargetType="DataGridRowHeader">
                    <Setter Property="Padding" Value="0, 10"/>
                    <Setter Property="Content" Value="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=AlternationIndex}" />
                </Style>
            </DataGrid.RowHeaderStyle>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Padding" Value="10,0"/>
                    <Setter Property="Height" Value="30"/>
                    <Setter Property="Background" Value="#d9d9d9"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="FontFamily" Value="{StaticResource Lexend}"/>
                    <Setter Property="FontSize" Value="10"/>
                    <Setter Property="Foreground" Value="#474747"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Width="*"/>
                <DataGridTextColumn Header="Date" Width="*"/>
                <DataGridTextColumn Header="Current" Width="*"/>
                <DataGridTextColumn Header="Previous" Width="*"/>
                <DataGridTextColumn Header="Change" Width="*"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
