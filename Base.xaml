<Window x:Class="DPC.Base"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="DPC - Dashboard" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Grid>
        <md:Card Background="#FDFDFD" UniformCornerRadius="8" Padding="25" md:ElevationAssist.Elevation="Dp4" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="SidebarColumn" Width="260" />
                    <ColumnDefinition x:Name="MainContentColumn" Width="*" />
                </Grid.ColumnDefinitions>

                <Border x:Name="SidebarContainer" Grid.Column="0" Background="Transparent"/>

                <Grid x:Name="MainContainer" Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <md:Card x:Name="TopNavBarContainer" Grid.Row="0" Background="#D9D9D9" Padding="10" Margin="0,0,0,9"/>

                    <md:Card Grid.Row="1" Background="#D9D9D9" Padding="10" UniformCornerRadius="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition x:Name="AIColumn" Width="0" />
                            </Grid.ColumnDefinitions>

                            <ContentControl x:Name="ViewContentControl" Grid.Column="0"
                                            Content="{Binding CurrentView}" />

                            <Border x:Name="AIPanelContainer" Grid.Column="1" Visibility="Collapsed" Margin="10,0,0,0">
                                <ContentControl x:Name="AIContentControl" />
                            </Border>
                        </Grid>
                    </md:Card>
                </Grid>
            </Grid>
        </md:Card>
    </Grid>
</Window>